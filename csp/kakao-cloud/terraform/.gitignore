# OpenTofu / Terraform
# .terraform.lock.hcl is deliberately NOT ignored — the provider hash pin has to
# be committed for a reproducible `tofu init`.
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfplan
crash.log
crash.*.log

# Generated scripts (auto-created by Terraform)
generated/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# SSH Keys
*.pem
*.key
id_rsa*
