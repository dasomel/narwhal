# Narwhal 사건 기록 (Lessons Log)

> `CLAUDE.md`의 Mistakes Log를 그대로 옮긴 아카이브. 매 턴 재전송되는 컨텍스트 파일에는
> **여러 사건에 걸쳐 일반화되는 규칙만** 남기고(→ `CLAUDE.md` "Recurring Rules"), 날짜별 사건
> 서술은 여기에 보관한다. 새 사건이 발생하면: 서술은 이 파일에, 일반 규칙이 도출되면 그것만
> `CLAUDE.md`에 추가.
>
> 항목 추가 형식: `| YYYY-MM-DD | 사건 설명 | 조치 |`

## Mistakes Log (Compounding Engineering)

> Add entries here whenever Claude makes a mistake. The same mistake will not be repeated.
> Request CLAUDE.md updates with the `@.claude` tag during code reviews.

### Shell Script Mistakes
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-08-09 | `show-credentials.sh` run against Kakao read **every secret correctly and then printed another cluster's node addresses**. `DOMAIN` was already a variable, but the final SSH section hardcoded the Vagrant values (`192.168.56.10-12`, the kube-vip VIP, MetalLB), so on Kakao it confidently reported a VIP that does not exist there and a subnet belonging to nothing. The danger is that it is not blank, it is *plausible* — nothing in the output invites doubt. Same failure shape as the stale bastion IP sitting in `~/.ssh/config`. Separately, running it against a halted cluster hung for **five minutes printing nothing**: ~25 secret lookups, each ~15s, because `--request-timeout=3s` still leaves kubectl's API-discovery retries in place | Add the `PROVIDER` branch, but do **not** hardcode node IPs on either side — read them from `kubectl get nodes`, so the next subnet change cannot stale them. Select workers with `-l '!node-role.kubernetes.io/control-plane'`: kubectl's jsonpath has no `!` filter, so `?(!(@.metadata.labels...))` silently yields nothing (the first attempt printed `Workers (unreachable)` against a healthy cluster). Kakao's public IPs come from the tfstate with `BASTION_IP`/`API_LB_IP`/`INGRESS_LB_IP` overrides, because this script also runs on master-1 where no repo checkout exists. Fix the slowness with **one reachability probe up front** (`kubectl get --raw /readyz`), not with per-call timeouts: 5 min → 3 s, and the failure now names the provider-specific cause (tunnel needed vs. VMs halted). Discriminator for the whole class: a script that takes a provider flag but embeds one provider's addresses does not fail on the other provider, it *lies* — grep for literal IPs in any script that has a `PROVIDER` branch |
| 2026-08-09 | Verifying the Korean CHANGELOG translation, `grep '^### ' file \| sort \| uniq -c` reported **14 of 15 section headings collapsed into `### 추가`** — a fabricated defect. macOS `uniq` compares with `strcoll` under `LANG=en_US.UTF-8`, and that collation treats `추가`/`수정`/`변경`/`보안`/`제거` as equal, so it merged five distinct headings into one count. `LC_ALL=C` on the same file gives the correct 5/4/3/1/1/1. I had already reported the defect to the user before re-checking | Pin `LC_ALL=C` on `sort`/`uniq`/`comm` whenever the input is non-ASCII — byte comparison is what these checks actually want, and locale collation silently redefines "equal". Discriminator: if a count check says lines are duplicates, print the lines themselves before believing it — a positional `paste <(grep -n …) <(grep -n …)` showed the true sequence immediately and cost nothing. Fifth time in this repo that a clever grep pipeline produced a false alarm, and the first where the false alarm was about a *correct* file rather than a broken one |
| 2026-08-05 | `if kubectl get pod ... \| grep -q "Running" \|\| true; then` — the `\|\| true`, added to dodge pipefail, made the condition **always true**, so the test it looked like it was performing never ran. Found while auditing the 15 remaining `cmd \| grep -q` sites after the chrony bug. Harmless here only by luck: the inner `kubectl wait` was the real gate | Delete the dead check rather than repair it — `kubectl wait` already returns non-zero on timeout, which is exactly what the loop wants, so a working outer test would only duplicate it. General form: `\|\| true` on a **condition** does not make it safe, it makes it meaningless; use it on statements, never on the thing being tested. The same audit measured the other 14 sites on a live cluster and found the failure rate tracks upstream output size — `kubectl get pods -A \| grep -q` 29/40, `kubectl get netpol -A` 2/40, `ip -o addr show` 1/40, `echo "$VAR" \|` 0/40 (a builtin finishes before grep exits). Two sites that looked like 40/40 failures were genuine non-matches, not SIGPIPE — the fourth time in this repo a clever grep produced a false alarm, so confirm a match exists before calling it a bug |
| 2026-08-05 | `WARN: chrony service not found after install` on a node where `chrony.service` was installed, enabled, and `NTPSynchronized=yes`. The test was `systemctl list-unit-files \| grep -q "^chrony\.service"`: `grep -q` exits on the first match, closing the pipe while systemctl is still writing its long unit table, systemctl takes SIGPIPE, and `set -o pipefail` turns rc=141 into a false negative. Measured 37/40 runs wrong. Second sighting of the pipefail/SIGPIPE trap (2026-08-04 was `apt-cache madison \| awk exit`, which killed all six nodes) — but that one died loudly, and this one did not | `systemctl cat "${c}.service"` — 0/40 wrong, no pipe at all. The lesson is the **inside-`if`** variant of the trap: at statement level pipefail kills the script with no message, but inside a condition nothing dies, the branch just doesn't run. Here everything downstream sat inside `if [ -n "${CHRONY_SVC}" ]`, so the false negative silently skipped the `makestep 0.1 -1` fix (D12) and the kubelet clock-sync gate, and the only evidence was a WARN line saying clock sync had been left to the hypervisor. Whenever `cmd \| grep -q` is used as a test, prefer a form with no pipe or capture first; the risk scales with how much the upstream still has to write |
| 2026-08-05 | Emptied this file. A one-liner did `open(p, "w").write("\n".join(lines) + "\n")` where `lines` held a tuple from a stray trailing comma: `open(p, "w")` truncates on open, the join then threw, and the write never ran. 224 rows of incident history were gone from the working tree, and the next two commits captured the empty file | Build the content first, write second — `data = render(); open(p,"w").write(data)`. `open(..., "w")` is destructive before a single byte is produced, so any expression that can throw must be evaluated before the file is opened. Recovered from git; the giveaway was a section-header lookup failing on a file that should have had five |
| 2026-08-04 | A clean run died at `03-cni-install.sh` on `curl: (35) OpenSSL SSL_connect: SSL_ERROR_SYSCALL` fetching helm. Probing the same URL five times immediately afterwards gave 1 failure and 4 successes — it was a transient TLS reset, not a misconfiguration. The script had no retry, so one flaky fetch failed the whole `init` stage and discarded a successful `kubeadm init` with it. `04-addons.sh` and `05-nfs-quota-agent.sh` fetched the same way | Reuse the `retry()` already in `03-k8s-install.sh` for every download that crosses the proxy to a public CDN. Distinguish a transient failure from a broken configuration by probing several times before changing anything — the previous helm failure at this same line WAS configuration (squid started before its resolver), and treating this one the same way would have chased a fix that was already correct |
| 2026-08-04 | The check added to stop that recurring flagged the two files it had just fixed: their `curl` sits inside a function invoked via `retry`, and the check grepped line by line. Third false alarm from a too-clever grep in this suite (R21 matched a comment, R22 missed a version-pinned install) | Make it coarse on purpose — "this file fetches from a public host and defines no `retry()`". A check nobody trusts is worse than a blunt one they do |
| 2026-08-04 | `up.sh`'s Phase 2 attempt dies once per clean install with `The SSH connection was unexpectedly closed by the remote end`, costing an attempt each time — a run burned all three and still needed a manual `vagrant provision master-1 --provision-with phase2-platform` to finish. **Not root-caused, and the tempting answers are wrong.** Cleared with evidence: sshd killed nothing (`clientaliveinterval 0`; its only disconnect records are up.sh's own probes closing client-side), no OOM on the node, the host never suspended (`pmset -g log` has no sleep since 2026-07-28, caffeinate holds the assertion), and the NAT DHCP lease renews every 15 min **without** changing the address. It also lands somewhere different every time — ztunnel's DaemonSet rollout in one run, cert-manager's install in another, and not at all in two more | Do not attribute it to whichever step it interrupted; with 2 drops across 4 runs at 2 different steps, that is overfitting, and an early write-up of this row blamed ztunnel on a single sample. Mitigations that hold regardless: Phase 2 attempts now genuinely resume (see the Helm 4 row), so a lost attempt is lost wall-clock rather than lost work, and the budget moves 3 → 5. `up.sh` milestones now carry timestamps — the reason this could not be correlated against the guests' journals is that vagrant's output has no clock, so the drop was visible in the log and invisible in time |
| 2026-08-04 | `K8S_PATCH_VERSION` looked like a pin and pinned nothing. It appeared in exactly one place — the banner — while the install ran `apt-get install -y kubelet kubeadm kubectl` unpinned, so a clean install announced v1.35.5 while every node came up on 1.35.7. Worse, `scripts/airgap/01-generate-image-list.sh` parses that same constant out of the Vagrantfile, so the offline bundle was assembled for a version nothing runs | Resolve the apt version by **prefix** (`1.35.7-1.1` — the packaging suffix changes without the Kubernetes version changing) and fail with the available list when the requested patch is not offered; `--allow-downgrades` is required or a node that already took a newer patch refuses to come back and the fleet ends up mixed. The general trap: a variable that is only ever echoed is not configuration, and an unpinned install makes "the clean install works" unfalsifiable — today's run and next month's are different clusters |
| 2026-08-04 | `up.sh`'s `phase2_complete()` ran `vagrant ssh master-1 -c "kubectl get ns X"` per namespace and read only the exit code, so an unreachable master-1 — the normal state right after the VMware race that had just killed the attempt — was reported as `Phase 2 incomplete: namespace 'X' not found`. It named `platform-system` twice while that namespace demonstrably existed, and it is the first line anyone reads when diagnosing a failed Phase 2 | Fetch the list once and decide locally, so **"could not ask" and "the answer is no" produce different messages**. Any check that collapses both into one exit code will eventually send its reader after the wrong thing; that cost real minutes here, hunting a namespace that was present all along |
| 2026-08-03 | Wiring the regression checks into CI, I tested one by reverting the fix it guards — and it still passed. `check R21 … grep -q 'mikefarah/yq'` matched the *comment* explaining the fix, not the download line implementing it, so deleting the install would have gone unnoticed. Nine more checks had the same shape: their pattern appeared in both code and comments | Anchor positive greps to non-comment lines (`grep -qE '^[^#]*PAT'`), and mutation-test the suite — delete the line each check matches and confirm the check fails. A check that has only ever been seen passing is not known to work; all ten now demonstrably catch removal |
| 2026-08-02 | `set-config-kakao.sh` exited 255 printing only "Opening tunnel...". Replacing the instances gave every node a new host key, and `StrictHostKeyChecking=accept-new` takes a NEW key but refuses a CHANGED one — with `ssh -f ... >/dev/null 2>&1` and `set -e`, the warning was discarded and nothing said why. The one failure guaranteed to follow a rebuild left no evidence at all | Drop the stale known_hosts entries for the nodes before connecting, and capture ssh's stderr to a file so a failure can print it. Never send the stderr of the command you are about to die on to /dev/null |
| 2026-08-02 | Checked whether phase 2 was still running with `pgrep -f 06-phase2-start` over ssh. The pattern matched the ssh command line carrying it, so a job that had finished 28 hours earlier reported RUNNING every time it was polled. Same shape as the `pkill -f '6443:127.0.0.1:6443'` that killed its own invocation earlier in this work | `pgrep`/`pkill` patterns are matched against every process including the one asking. Confirm from the artefact instead — the log's last line and its mtime said "finished" plainly. This has now bitten three times in one exercise (`pkill -f '6443:…'` killing its own invocation, phase 2 reported RUNNING for 28 hours, and `pgrep -cf` counting the ssh that carried it), so treat any process-name check over ssh as wrong by default |
| 2026-08-01 | Two sessions worked this repo at once and neither noticed. Three lessons-log rows written by one landed in `e82d8e1`, a commit from the other whose message describes only a CLAUDE.md change — a broad `git add` swept up edits sitting uncommitted in the tree. The rows survived intact, so nothing looked wrong; the damage is that `git log` now misdescribes them. Compounding it, `git log` authorship cannot separate the sessions at all: `user.name` is `Antigravity` for every commit in this repo, and a claim was made in review that a commit came from "another agent" on exactly that non-signal | Stage the paths your task touched, never `git add -A`, and re-check `git status` immediately before committing — in a repo with concurrent sessions the tree is not yours alone. To tell work apart use content and timing, not `%an`. If a `grep -c` for your own change returns 0, suspect the pattern before suspecting a revert: `$2` and `(` inside a shell-quoted regex nearly produced a false "my fix was reverted" alarm here — `grep -F` settles it |
| 2026-08-01 | RCA of the above found the worse half: `cluster-heal.sh` **certified its own damage as healed**. It wiped the CNI at 23:38:37, then its convergence poll — counting STATUS only, against a `<=3 non-running` threshold with no stated basis — declared "Cluster converged: 3 non-running pods" at 23:41:45 and exited 0. `seaweedfs-volume-0`, sitting at `0/1 Running`, was never counted. Every deletion is `--force --grace-period=0`, so a single mis-evaluated predicate goes straight to irreversible with no ceiling on blast radius. Kin to the 2026-07-30 `registry`-stage row: both report success over a failure they caused | A repair tool must not be its own judge. Bound the damage before trusting the judgment: refuse to delete a whole DaemonSet in one pass, prefer graceful deletion, and make the success threshold something a reader can check (why 3?). Note also what the log looks like when this misfires — `Deleting unhealthy Cilium pod` reads as the healer working, which is why it survived a month unquestioned |
| 2026-08-01 | CI shellchecks `find scripts/` only, so nothing under `tests/` has ever been linted — `tests/k6/run-k6.sh` carried two SC2155 warnings no one had seen. Widening the glob would fail CI on those pre-existing warnings, which is why the gap persists: the cheap fix is blocked by what it would reveal | Widen the lint scope and fix what it surfaces in the same change, or the gap is permanent by construction. When adding a check, confirm which paths it actually covers — `scripts/` looked like "all scripts" to everyone who read the workflow |
| 2026-07-31 | **Three of the four pod filters in `cluster-heal.sh` had the wrong field indices**, and the class failed in both directions. Failing OPEN: the Cilium filter read a `-n kube-system` query with the `-A` layout — `awk '$3!="1/1" && $3!="2/2" \|\| $4!="Running"'` — so it compared STATUS to `"1/1"` and RESTARTS to `"Running"`, both always true, and the next line force-deleted all 6 healthy cilium pods plus the operator. Confirmed once in journald (2026-07-29 23:38:37, one second after the service started); the service has been enabled since 2026-07-01 and runs at every master-1 boot, but journald retains only two boots so earlier runs are unevidenced — an initial "every run for months" claim was not supportable. Failing CLOSED: the istio-system and platform-CrashLoop blocks matched `$4` against STATUS values in single-namespace listings, so neither had deleted anything in its entire life. The `1/1 or 2/2` allowlist was a third defect underneath, since this cluster runs 3/3 and 5/5 pods | `kubectl get pods` shifts every column by one between `-n <ns>` (NAME READY STATUS) and `-A` (NS NAME READY STATUS) — never copy a field index between the two forms, and audit *every* filter in a file once one is found wrong. The fail-closed half is the harder lesson: a block that never fires produces no symptom at all, and its neighbours' log lines get read as proof it works — the "Deleting Unknown Istio pod" entries everyone attributed to the istio block came from the cluster-wide sweep. The 2026-07-30 rule below would have caught the open case: run the checker against a known-good subject and confirm it selects **nothing**. For the closed case the inverse is needed — confirm it selects **something** when you break a subject on purpose |
| 2026-07-30 | Three checks in a new regression script failed on a healthy cluster. All three were the check's fault: two matched a pattern inside a comment that existed to explain why the pattern is *not* used, and one called a pod Terminating during a rollout the signature of a wedge | Validate a checker against a known-good subject before trusting a failure. A check that flags a comment, or normal transient state, teaches its reader to skip it — which costs more than the check ever earns |
| 2026-07-30 | A repo-wide link-rewrite script matched every `*.md` under the tree and edited `00.bak/`, `poc/`, and a vendored chart. Reverting was easy only because the changes were uncommitted | Scope a bulk rewrite to a file list before running it, not to "everything that parses". Backup and vendor directories look exactly like source to a glob |
| 2026-07-30 | Moving docs into subdirectories broke two classes of link, and the first pass fixed only one. Links to other docs were repointed by basename, but every `../` path out of `docs/` was now one level short — and those failed silently because the rewriter had no rule for them | After a move, resolve every link against the filesystem and fail on the ones that do not exist. Rewriting what you predicted will break leaves behind what you did not |
| 2025-01-27 | Script written without `set -e` | Always add `set -euo pipefail` as the first line |
| - | Variable expansion error in heredoc | Use `<<'EOF'` (quoted) to prevent variable expansion, `<<EOF` to allow it |
| - | Missing `-y` flag in apt commands | Always use `apt-get install -y` |

### Kubernetes/Helm Mistakes
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-08-09 | Prometheus scraped node-exporter on only 1 of 6 Kakao nodes and missed kube-controller-manager (:10257) / kube-scheduler (:10259) entirely, producing 9 of the 15 firing alerts. **The confident first diagnosis was wrong.** A diagnosis lane found ztunnel `io error: deadline has elapsed` in the ambient-enrolled `monitoring` namespace, called it "Confirmed Cause: ztunnel egress interception", and the `istio.io/dataplane-mode: "none"` opt-out was deployed — the recreated pod carried the label and **every target stayed down with identical `context deadline exceeded`**. Real cause: the Kakao security group allows `10251-10252` (the pre-1.17 metric ports, a fossil matching nothing) but not `10257/10259`, and has no `9100` at all; Kakao SGs drop rather than reject, so blocked scrapes look like timeouts, and ztunnel's error was the same drop seen one layer up | Three additive SG rules in `csp/kakao-cloud/terraform/modules/security/main.tf` (9100, 10257, 10259 from the VPC CIDR); `tofu plan` showed 0 add/1 change/0 destroy and all 11 targets recovered within 90s of apply, alerts 15→5. Discriminators that separate SG-drop from ztunnel-intercept — both produce "only the local node works": (1) **deploy the opt-out and re-test** — if the opted-out pod still times out, the mesh was never the blocker; (2) kubelet `:10250` scraping cross-node all along — ztunnel does not exempt 10250, but the SG explicitly allows it; (3) timeout-not-refused on a cloud provider whose SGs drop. The ambient opt-out was kept (an in-mesh scraper adds an interception layer for no benefit) but it is hardening, not the fix. "The evidence supports my hypothesis" is not "the evidence rules out the others" — the ztunnel log line was a symptom shared by both theories |
| 2026-08-09 | The narwhal portal compliance page displayed "infra failures: 0" and "nodes scanned: 0" as if clean, but `clusterinfraassessmentreports` existed for all 6 nodes. The portal ClusterRole granted read access only to namespaced `infraassessmentreports` instead of cluster-scoped `clusterinfraassessmentreports`, so the K8s API returned forbidden/empty results which rendered as zeroes | Grant `clusterinfraassessmentreports` (and check/add other cluster-scoped variants like `clusterexposedsecretreports` and namespaced `compliancereports`) in `narwhal-portal-k8s.yaml`. Discriminator: A UI dashboard showing "0" or "clean" can mean "no RBAC permission to list the resource" rather than "no findings exist". Compare namespaced vs cluster-scoped variants of CRDs to identify missing ClusterRole permissions |
| 2026-08-04 | After retrying the helm binary download, the same script died one step later: `cilium install` reaches helm.cilium.io for the chart itself and got `context deadline exceeded`, while curl to that host answered 200 five times a minute afterwards. Guarding one fetch had just moved the failure to the next unguarded one — the Gateway API `kubectl apply -f https://…` calls were equally exposed | Retry the whole `cilium install`, guarded by a check for an existing DaemonSet so a retry after a partial install skips rather than failing on "already exists", and retry the five CRD applies. When a flaky network breaks one call in a stage, the fix is every call in that stage, not the one that happened to fail |
| 2026-08-04 | `11-4-keycloak-apiserver.sh` died on its **first** command — `kubectl create namespace dev` — with `failed to download openapi: dial tcp 192.168.56.10:6443: connect: connection refused`, and being marked `critical` it took all of Phase 2 down with it. The API server was absent because kubelet had just started at 19:39:20 and was still bringing the static pods back. **Ruled out with evidence, not assumed:** boot-heal logged `No wedge detected` through the whole window so it did not restart anything; no `vagrant reload master-1` appears in the run log; sshd shows no session it killed (`clientaliveinterval 0`); zero OOM events; and `pmset -g log` records **no sleep since 2026-07-28**, so the host did not suspend. What triggered the kubelet start is still unidentified — vagrant's output has no timestamps to correlate against, which is itself the reason this took so long | Gate on the API server before the first kubectl, the way `07-cnpg.sh` already does. A control-plane blip during Phase 2 should cost a wait, not the install. The general rule: **any `critical` script whose first action is a kubectl needs a readiness loop**, because its failure is not local — it aborts everything downstream. Did not reproduce on re-run, so the trigger stays open; the gate makes it harmless either way |
| 2026-08-04 | **Helm v4.2.1 made server-side apply the default and deprecated `--force`, which broke every Phase 2 RE-install while leaving first installs fine.** Three components failed on the upgrade path: `apisix` (`--force` is now mutually exclusive with SSA), `cert-manager` (webhook `.rules` owned by `kubectl-client-side-apply`), `openbao` (webhook `.clientConfig.caBundle` owned by `vault-k8s`). Fourteen retry loops made it invisible: `for attempt in 1 2 3 4 5` broke on success and simply fell through on exhaustion, so five failures read as installed. APISIX and cert-manager both exhausted all five this run and were saved only by already existing from an earlier attempt | Add `--force-conflicts` to every helm upgrade (inert unless there is a conflict) and drop `--force`; record success per loop and exit non-zero when the retries run out. Why this matters more than a flag: `up.sh`'s entire resilience strategy is "Phase 2 is idempotent, retry it", and the VMware SSH race makes retries routine — a broken re-install path therefore means the install cannot finish at all. **The 2026-02-10 MetalLB row below saw this exact pattern and called it harmless** ("initial install is fine, re-upgrade has CRD caBundle conflict, pods function normally"); that judgement is why it was still here to spread to three more components |
| 2026-08-03 | `cluster-heal.sh`'s cluster-wide Unknown sweep force-deleted `database/narwhal-db-1` after an unclean power-off. That pod was the CNPG **primary**, mid-bootstrap, and its removal deadlocked the operator: `Cluster narwhal-db  2 instances, ready=0, "Creating a new replica"`, `ConsistentSystemID=False — No instances are present to report a system ID`, while `narwhal-db-2-join` looped forever on `narwhal-db-rw:5432 connection refused` because no primary existed for it to stream from. `07-cnpg.sh` then failed its 10-minute wait on all three Phase 2 attempts — the deadlock is persistent and `up.sh` cannot retry its way out. Self-inflicted context: a clean install had been killed mid-CNPG-bootstrap, so the cluster was in a state a real install never reaches | Deleting an Unknown pod is right for a stateless workload and can be destructive for a stateful one — the healer has no notion of the difference, and `--force --grace-period=0` on a database primary is the worst case of that. Before blaming the installer, check whether the boot healer removed something it should have left: `journalctl -u narwhal-cluster-heal -b \| grep Deleting`. Recovery is to delete the Cluster CR and let the bootstrap re-run; there is no way to promote a replica that never finished joining |
| 2026-08-02 | 14 ArgoCD applications sat at `Unknown` sync (but `Healthy`) with `failed to list refs: ... read: connection reset by peer`. Two gitea pods were running from two ReplicaSets: the chart ships `RollingUpdate` with `maxSurge=100%`, and 12-gitea.sh patches the pod template, so an update briefly wants two pods. Gitea's data volume is RWO and its LevelDB queue takes an exclusive lock, so the second pod crashlooped on `unable to lock level db at /data/queues/common` (36 restarts) — while the Service kept both endpoints, sending roughly half of ArgoCD's git traffic to the dead one | Patch `strategy` to `Recreate` in the same call as the label, so only one rollout happens and the old pod stops before the new one starts. A workload whose volume admits one writer must never be given a strategy that asks for two — and the symptom shows up in an unrelated component, not in gitea |
| 2026-07-29 | `seaweedfs-volume-0` sat at `0/1 Running` with 11 restarts after a reboot and was missed twice: a `$4!="Running"` filter never flags a Running-but-not-Ready pod, and every other pod was healthy. It silently broke the data path — the master logged `No writable volumes` / `Not enough data nodes found!` for collection `loki`, so Loki was `2/2 Running` while unable to persist anything. Cause: the pod's ambient redirection was not applied when it started (istio-cni loses the race after a reboot), so its outbound went to the master's pod IP as plaintext and the master's inbound ztunnel denied it under mesh-wide STRICT | Read the ztunnel access log and compare a working peer with the failing one — `src.identity="spiffe://..."` plus `dst.hbone_addr` present means HBONE; **absent `src.identity` with a direct `dst.addr` is a pod that was never redirected**, and the giveaway error is `policy rejection: explicitly denied by: istio-system/istio_converted_static_strict`. Fix is to delete the pod so istio-cni re-applies redirection — not to add a `dataplane-mode: none` opt-out, which treats an enrollment race as a permanent exemption. Check readiness with `READY` columns (`r[1]!=r[2]`), never `STATUS` alone |
| 2026-07-29 | `loki-0` CrashLoopBackOff (12 restarts, no self-recovery) after halting and re-booting the VMs: `err="unexpected EOF" / error initialising module: store`. The tsdb-shipper creates each WAL segment before writing to it, so stopping the VM inside that window leaves a 0-byte segment on the NFS export that every later start chokes on. The memberlist `no such host` warnings in the same log are a red herring — they resolve once loki is up | Delete the 0-byte segment (lossless: no log data by definition, chunks live in SeaweedFS and `chunks/`) plus `tsdb-shipper-cache`, then delete the pod — 2/2 Running in 57s. Automated in `boot-heal.sh`. Note a healthy running loki ALWAYS has a 0-byte segment open, so size alone must never be the trigger: gate on `! -newermt "$(uptime -s)"` so only pre-halt leftovers match |
| - | Missing namespace creation | Use `--create-namespace` or `CreateNamespace=true` syncOption |
| - | Ignoring CRD dependencies | Create CR after Operator installation, use ArgoCD sync-wave |
| - | Attempted to resize PVC | PVC can only be expanded, not shrunk |
| 2026-01-30 | OAuth2 Proxy cookie-secret empty value | `cookieSecret: ""` causes error, generate exactly 32 bytes with `openssl rand -hex 16` |
| 2026-01-30 | OAuth2 Proxy service.port deprecated | Use `service.portNumber` (v7.x chart) |
| 2026-01-30 | Keycloak issuer URL mismatch | `insecure_oidc_skip_issuer_verification = true` or match Keycloak hostname setting |
| 2026-01-30 | Loki chunks/results cache failure | `chunksCache.enabled: false`, `resultsCache.enabled: false` for dev environment |
| 2026-01-30 | Headlamp Helm repo URL changed | Use `https://kubernetes-sigs.github.io/headlamp/` |
| 2026-01-30 | Keycloak service name typo | `keycloak-service` (not `keycloak`), specify port 8080 |
| 2026-02-04 | YAML parsing error when modifying with sed | Use `yq` for safe YAML modification (e.g., API server manifest OIDC config) |
| 2026-02-05 | Helm `--set` nodeSelector boolean error | Use `--set-string nodeSelector.key=true` (force string) |
| 2026-02-14 | kube-vip `vip_subnet` value contains `/` | Use `"32"` (NOT `"/32"`), internally combines `address + "/" + vip_subnet` |
| 2026-02-14 | kube-vip kubeconfig path not recognized | Mount at `/.kube/config` (distroless image HOME=/) |
| 2026-02-14 | kube-vip admin.conf VIP circular dependency | Create separate `kube-vip.conf`, change server address to local IP |
| 2026-02-14 | kube-vip static pod created before init (chicken-and-egg) | master-1 creates manifest after init, bootstrap with manual VIP binding |
| 2026-02-14 | kubeadm join detects VMware NAT interface | `--apiserver-advertise-address=192.168.56.x` must be specified |
| 2026-02-14 | VMware Vagrant private_network IP not assigned | Create netplan directly in `01-prerequisites.sh`, `chmod 600` required |
| 2026-02-14 | API server OOM restart on Master 4GB RAM (during platform apps installation) | Current topology: Master has NoSchedule taint (control-plane only, 4GB OK), Worker 6GB runs platform apps |
| 2026-06-07 | Master 4GB still too tight at steady state: apiserver ~2GB (no mem limit) + mandatory DaemonSets ~0.8GB → ~567Mi headroom on master-3 → apiserver SIGKILL (exit 137 via /livez timeout) + intermittent NodeNotReady flapping (also broke metrics-server → portal CPU/mem blank) | **Fixed**: bumped `MASTER_MEMORY` 4096→6144 in Vagrantfile. Apply: rolling `vagrant reload` one master at a time. Partial: exclude falco DaemonSet from masters |
| 2026-02-14 | ArgoCD v3.x applicationsets CRD exceeds 262KB | Use `kubectl apply --server-side --force-conflicts` |
| 2026-02-14 | Helm `--wait` causes release rollback on timeout | Remove `--wait` for non-critical apps, use `--timeout` only |
| 2026-02-15 | Velero CRD hook musl/glibc incompatibility (ARM64) | Set `upgradeCRDs: false`, alpine/k8s musl cannot execute in velero glibc container |
| 2026-02-15 | registry.k8s.io/kubectl is distroless (no shell) | Use `docker.io/alpine/k8s:1.31.4` (includes shell+kubectl) |
| 2026-02-15 | Traefik routes applied before GatewayClass created | Wait loop for Traefik deployment+GatewayClass before applying routes |
| 2026-02-15 | etcd container cannot execute `sh -c` | etcd is distroless, call `kubectl exec -- etcdctl ...` directly |
| 2026-02-15 | alpine/k8s image tag has no `v` prefix | Use `"1.31.4"` (NOT `v1.31.4`), verify Docker Hub tag format |
| 2026-02-15 | ArgoCD v3.x `server.insecure` wrong location | Must set in `argocd-cmd-params-cm` (NOT `argocd-cm`). `argocd-cm` is legacy |
| 2026-02-15 | worker/master-2 DNS resolves to public IP | Set `Domains=~local.narwhal.internal` in `systemd-resolved` + use master dnsmasq as DNS |
| 2026-02-15 | Keycloak `groups` scope missing -> `invalid_scope` error | Create realm-level `groups` client scope + add mapper + assign as default scope to all clients |
| 2026-02-15 | ArgoCD SSO `x509: certificate signed by unknown authority` | Add `oidc.tls.insecure.skip.verify: "true"` to `argocd-cm` (self-signed cert) |
| 2026-02-15 | Headlamp `extraArgs` ignored at root level | Must place in `config.extraArgs` for container args injection |
| 2026-02-15 | Harbor `configureUserSettings` change rejected by API | Injected via env vars so cannot modify via API, change Helm values + redeploy |
| 2026-02-18 | Cilium + Istio CNI conflict (cni.exclusive defaults to true) | `cni.exclusive=false` required, prevents Cilium from deleting other CNI configs |
| 2026-02-18 | Cilium socket LB bypasses ztunnel traffic | `socketLB.hostNamespaceOnly=true` required, prevents mesh traffic from bypassing ztunnel |
| 2026-02-18 | Istio CRD annotation exceeds 262KB (ArgoCD) | Add `ServerSideApply=true` to ArgoCD syncOptions |
| 2026-02-18 | Istio 1.28 does not support K8s 1.35 | Use Istio 1.29.x (supports K8s 1.31~1.35) |
| 2026-02-18 | Istio images docker.io only (no alternative) | Docker OSS rate limit exempt, `docker.io/istio/*` usage unavoidable |
| 2026-02-19 | CiliumClusterwideNetworkPolicy `endpointSelector: {}` blocks all traffic | Empty endpointSelector selects all pods -> deny-by-default activated. Never use empty selector in CCNP |
| 2026-02-19 | CoreDNS `forward . /etc/resolv.conf` dnsmasq loop | Master's resolv.conf is 127.0.0.1 (dnsmasq) -> CoreDNS internal loop. Use `forward . 8.8.8.8 8.8.4.4` explicitly |
| 2026-02-19 | Istio ambient HBONE port 15008 blocked by NetworkPolicy | Ambient mesh uses HBONE (15008) between pods. Must add 15008/TCP to NetworkPolicy ingress |
| 2026-02-19 | OAuth2-Proxy `insecure_oidc_skip_issuer_verification` does not skip TLS verification | Only skips issuer, TLS verification is separate. Need to add `ssl_insecure_skip_verify = true` |
| 2026-02-19 | Traefik Gateway API CRD field manager conflict | Extract chart CRDs -> apply with `--server-side --force-conflicts` -> `helm install --skip-crds` |
| 2026-02-24 | Keycloak Operator auto-generated NetworkPolicy missing HBONE 15008 | Operator manages `keycloak-network-policy` so cannot modify directly. Create separate `keycloak-allow-hbone` NetworkPolicy for 15008/TCP (see `11-keycloak.sh` pattern) |
| 2026-02-24 | Keycloak hostname v1/v2 option conflict | Do not use `hostname-url` (v1 deprecated) in `additionalOptions`. v2: `hostname.hostname` + `hostname.strict: true` + `proxy.headers: xforwarded` |
| 2026-02-24 | yq inserts shell quotes into URL -> API server crash | Use `yq -i ".spec... += [\"--flag=value\"]"` (NOT `'...'`). Use double quotes for outer quoting when shell variable expansion needed |
| 2026-02-24 | HTTPRoute does not exist when adding API server OIDC flags | Must create Keycloak HTTPRoute before OIDC verification in `11-keycloak.sh`. Runs before GitOps bootstrap (14) |
| 2026-02-25 | Keycloak OIDC token `aud` claim only contains `account` (K8s API server Unauthorized) | Must add audience mapper to Keycloak `kubernetes` client: `oidc-audience-mapper`, `included.client.audience=kubernetes`. Without it: `oidc: expected audience "kubernetes" got ["account"]` error |
| 2026-02-25 | API server `--oidc-ca-file` missing -> JWKS verification fails with self-signed cert | `--oidc-ca-file=/etc/kubernetes/pki/oidc-ca.crt` required when Keycloak uses self-signed cert. Extract cert: `openssl s_client -connect ... \| openssl x509 -outform PEM` |
| 2026-02-25 | `kcadm.sh --format csv --noquotes \| tail -1` returns wrong ID | CSV format has unstable result order/fields. Use `kcadm.sh get ... 2>/dev/null \| jq -r '.[] \| select(.name=="X") \| .id'` instead |
| 2026-02-25 | `kubectl --token=X` does not override kubeconfig client-cert | X.509 auth takes priority over token. For OIDC testing use `KUBECONFIG=/dev/null kubectl --server=... --certificate-authority=... --token=...` |
| 2026-02-25 | Istio ambient mesh corrupts SSO web server cookies -> `http: named cookie not present` | Web servers in ambient namespace (ArgoCD, Grafana, Harbor, Gitea, OAuth2-Proxy, Headlamp) must opt-out with `istio.io/dataplane-mode: none` pod label. ztunnel HBONE corrupts Set-Cookie/Cookie headers |
| 2026-02-25 | Gitea OAuth2 source name case mismatch -> `/user/oauth2/Keycloak` 500 error | URL path source name is case-sensitive. Register with `gitea admin auth add-oauth --name "keycloak"` (lowercase) to access via `/user/oauth2/keycloak` |
| 2026-02-25 | `microprofile-jwt` scope has duplicate `groups` claim mapper | Default Keycloak `microprofile-jwt` scope already maps realm-role to `groups`. After creating custom `groups` scope, delete the `groups` mapper from other scopes |
| 2026-02-25 | All Keycloak clients `aud` claim only contains `["account"]` | Must add audience mapper to **all OIDC clients**, not just `kubernetes`. ArgoCD and other apps that verify tokens directly will get `expected audience "argocd" got ["account"]` error |
| 2026-02-26 | Keycloak user `emailVerified=false` -> OAuth2-Proxy 500 error | Add `-s emailVerified=true` to `kcadm.sh create users`. Or add `insecure_oidc_allow_unverified_email = true` to OAuth2-Proxy |
| 2026-02-26 | Traefik Errors middleware preserves 401 status code -> browser auto-redirect fails | Use nginx+JS redirect page instead of ExternalName->OAuth2-Proxy. Force redirect with `window.location.href` |
| 2026-02-26 | Traefik blocks ExternalName services by default | `providers.kubernetesCRD.allowExternalNameServices: true` required. Without it: 404 `externalName services not allowed` |
| 2026-02-26 | OAuth2-Proxy PKCE conflict (concurrent multiple OAuth flows) | Multiple protected apps redirect simultaneously -> code_verifier conflict. Add sessionStorage 5-second debounce in JS |
| 2026-02-26 | Traefik LB annotation `io.cilium/lb-ipam-ips` ignored by MetalLB | For MetalLB use: `metallb.universe.tf/loadBalancerIPs: "IP"` |
| 2026-06-11 | Keycloak Operator (26.6.1) **IGNORES resources set under `unsupported.podTemplate`** — the running pod had no effective CPU request (operator defaults: mem 2Gi/1700Mi, no cpu) regardless of the 200m in podTemplate. On a CPU-saturated node Keycloak got starved → its `:9000` health endpoint missed the operator's **1s** liveness `timeoutSeconds` → SIGKILL (exit 137) crash loop → `keycloak-service` endpoints emptied → APISIX `502` on ALL `keycloak.local.narwhal.internal` → cluster-wide SSO outage. (Contrary to the older note: in 26.6.1 podTemplate **probes ARE honored**; it's **resources** that are dropped.) | Put CPU/memory in the **first-class `Keycloak.spec.resources`** (operator honors it — verified pod got `cpu req 600m`). Relax probes via `unsupported.podTemplate` containers[0] `livenessProbe/readinessProbe/startupProbe` (`timeoutSeconds: 5`, higher `failureThreshold`) — honored. Immediate recovery: `kubectl cordon <hot-node> && kubectl delete pod keycloak-0 -n iam` to reschedule onto a node with CPU headroom, then uncordon. |
| 2026-06-12 | velero-ui native Keycloak OAuth: browser-side Keycloak login SUCCEEDED but `/api/auth/oauth` returned 500 → bounced to `/login?state=error&reason=sso`. Cause: velero-api (Node/passport-oauth2) makes a **server-to-server** call to the Keycloak token endpoint and rejected the private-CA TLS cert (`UNABLE_TO_VERIFY_LEAF_SIGNATURE`). Node trusts only the public bundle — mounting the CA for the *browser* path is irrelevant. Also: velero-ui SPA only completes the OAuth code exchange for a `state` matching `localStorage['auth.oauth.state']` → a cross-origin Keycloak authorize deep link can't work; zero-click requires a SAME-ORIGIN bootstrap page that seeds that key first (APISIX `serverless-pre-function` `/sso` route). Same pattern for OpenBao: `/sso` + `/sso/callback` pages drive the OIDC API flow and write the token as `localStorage['vault-token☃1']` in the UI's format (needs the extra redirect URI in BOTH the oidc role and the Keycloak client) | Mount `narwhal-ca-cert` secret + set `NODE_EXTRA_CA_CERTS=/etc/narwhal-ca/ca.crt` in chart values (`gitops/apps/velero-ui.yaml`). Verify in-pod: `node -e 'fetch(tokenUrl,{method:"POST"})'` → HTTP 400 (TLS ok) not TLS error. Deploy via Gitea push (kubectl apply gets reverted by app-of-apps selfHeal). Zero-click bootstrap routes: `apisix-routes.yaml` `openbao-sso`/`velero-ui-sso`; APISIX IC may need a restart to translate new ApisixRoute rules |
| 2026-06-12 | Harbor OIDC auto-onboard fails for Keycloak user `admin`: "failed to create user record: user admin or email ... already exists" — Harbor reserves the built-in local `admin`, so a same-named OIDC user can never onboard. ALSO: Keycloak 24+ **silently drops** user attributes set via admin API unless the realm user-profile allows them (`unmanagedAttributePolicy` unset = blocked) — a protocol mapper for the attribute then emits nothing and Harbor falls back to the `name` claim (onboards as `admin_narwhal` from full name "admin narwhal") | Dedicated username claim: realm user-profile `unmanagedAttributePolicy=ENABLED` (11-2), per-user attribute `harbor_username` (admin→`narwhal-admin`, others→own username), `oidc-usermodel-attribute-mapper` on the harbor client, Harbor `oidc_user_claim=harbor_username` (11-3). Harbor admin rights come from `oidc_admin_group=cluster-admin`, not the username. Delete any half-onboarded user via `DELETE /api/v2.0/users/<id>` before re-login |
| 2026-06-11 | trivy-operator (chart 0.27.0) operator/scanner config is read from **ConfigMaps, not the Deployment env** — `OPERATOR_CONCURRENT_SCAN_JOBS_LIMIT` lives in cm `trivy-operator-config`, scan-job resources (`trivy.resources.*`) in cm `trivy-operator-trivy-config`. Checking `deploy/trivy-operator` env (only has 5 `OPERATOR_*` basics) or the `trivy-operator` cm (scanJob template only) makes a correctly-applied value look missing. | Verify trivy runtime settings via `kubectl get cm trivy-operator-config` / `trivy-operator-trivy-config -n security-system`, not the deploy env. |
| 2026-06-30 | APISIX (chart v2.13.0) shipped global TLS OFF — bootstrap helm values in `08-1-networking.sh` lacked `apisix.ssl.enabled` → rendered config.yaml had no ssl block, 443 never served → Harbor push EOF, OIDC endpoint unreachable, Kaniko fail, portal ImagePullBackOff (D2) | Add `apisix.ssl.enabled: true` + `containerPort: 9443` to the values heredoc; idempotently ensure 443→9443 on apisix-gateway svc (chart bug). GitOps narwhal-apps chart already had ssl.enabled. (5492c1c) |
| 2026-06-30 | cert-manager root CA (`narwhal-root-ca`) timed out: kube-apiserver (hostNetwork static pod) intermittently can't reach cert-manager-webhook ClusterIP:443 (Cilium socketLB hostNamespaceOnly) → every cert-manager admission webhook call fails (D5) | In `08-1`: roll out all 3 cert-manager deploys, temporarily delete the validating/mutating webhook configs, create issuer+root-CA+wildcard with explicit Ready-polls, restore webhooks; trap on EXIT guarantees restore. (7f04fd3) |
| 2026-06-30 | metrics-server applied then patched TWICE (insecure-tls, then probes) → two ReplicaSet rollouts; a Phase-2 `kubectl wait ... pod -l k8s-app=metrics-server` matched the transient new pod and timed out → recurring clean-install abort (D10) | Merge both patches into ONE `kubectl patch`, then `kubectl rollout status deployment/metrics-server` (non-fatal) so Phase 1 only finishes once it settles to a single Ready pod. (d54d3c8) |
| 2026-06-30 | keycloak-0 flaky crashloop "FATAL: password authentication failed for user keycloak": the keycloak DB role password was set by BOTH `07-cnpg` (`narwhal-db-credentials/keycloak-password`) and `11-keycloak` (its own generated value) → last-writer-wins divergence vs the secret Keycloak uses (D9) | Make `07-cnpg` the single source: `11-keycloak` no longer generates a password or CREATE/ALTERs the role/db; it reads `narwhal-db-credentials/keycloak-password`, syncs `keycloak-db-secret`, and gates on a real psql auth check (10 retries) before deploying. (442985a) |
| 2026-07-10 | Kyverno Enforce validate pattern WITHOUT `=()` optional anchors (`securityContext: privileged: "!true"`) requires the field to EXIST → every pod lacking an explicit `privileged` field (i.e. most pods) was denied at admission in non-excluded namespaces; surfaced as velero-ui ReplicaSet `FailedCreate`, and would have blocked ALL rollouts/reschedules (reboot = cluster-wide outage) | In Kyverno patterns, always wrap optional fields in `=()` anchors: `=(securityContext): =(privileged): "false"` (upstream-standard form, also cover `=(initContainers)`/`=(ephemeralContainers)`). Verify both directions with `kubectl run --dry-run=server`: plain pod admitted AND privileged pod denied. (e898f59) |
| 2026-07-13 | GitOps portal pin bumps silently NOT deploying while the app showed Synced: argocd-cm carried a GLOBAL `ignoreDifferences.apps_Deployment` ignoring the narwhal-portal container `.image`/`.resources` (added for skaffold live-dev so selfHeal wouldn't revert dev images). With immutable SemVer pins this made every image-only bump invisible — bumps only landed by piggybacking on unrelated changes in the same sync. Extra trap: argocd-cm has a self-ignore on its own `.data` customization keys, so fixing the rule in git alone never reaches the live cm — must ALSO `kubectl patch` argocd-cm | Make the ignore CONDITIONAL on a dev image actually running: `select(.name=="narwhal-portal") \| select(.image \| test(":dev-")) \| .image` (and same for `.resources`) in `argocd-config.yaml` + live-patch argocd-cm. Verified: 1.0.14 pin synced in 10s after the patch. When an app is Synced but live differs from git, grep argocd-cm `resource.customizations.ignoreDifferences` before blaming sync machinery |
| 2026-07-13 | Recurring "argocd-redis EOF wedge" root-caused: `13-argocd.sh` opted server/repo-server/controller/notifications out of ambient but NOT `argocd-redis` → redis stayed mesh-enrolled while all its clients are non-mesh → under STRICT mTLS ztunnel drops their plaintext connections → repo-server "failed to list refs: EOF" / "retrieve git references from cache: EOF", Gitea-sourced apps stuck Unknown, gitops pin bumps never sync. Restarting redis/repo-server only helped while ztunnel enforcement flapped — fresh pods failed identically | Add `istio.io/dataplane-mode: none` to the argocd-redis Deployment pod template (13-argocd.sh + live patch). Verified: repo-server EOF 68/2min → 0 immediately. Same non-mesh-client→enrolled-server pattern as the STRICT mTLS exceptions lesson — when opting an app's clients out of ambient, opt out its backing services too |
| 2026-07-13 | Portal login → 502 on `/api/auth/callback/keycloak`: portal 1.0.11 started storing Keycloak access+refresh tokens in the NextAuth JWE → session cookie chunked into 2+ ~4KB `Set-Cookie` headers → response headers exceeded nginx's default `proxy_buffer_size 4k` → APISIX "upstream sent too big header" → 502; the browser retry then hit `invalid_grant: Code not valid` (code already consumed). Initially misdiagnosed as a Keycloak-restart blip — the APISIX **error** log (not access log) had the real cause | Raise proxy buffers in apisix `fullCustomConfig` → `nginx_config.http_configuration_snippet`: `proxy_buffer_size 16k; proxy_buffers 8 16k; proxy_busy_buffers_size 32k;`. For any gateway 502 on an auth callback, check APISIX error log for "too big header" FIRST. Verify with a scripted curl login (csrf→signin→Keycloak form→callback) expecting 302 + chunked session cookies |
| 2026-07-10 | Added a SECOND `image:` block to an ArgoCD Application inline `values:` string (helm values YAML) — duplicate map key silently resolved last-wins → the original block's `tag: "0.10.1"` was dropped, image fell back to chart appVersion (0.10.0 rollback). Also: kyverno `add-default-securitycontext` mutate injects `runAsNonRoot: true`; images with a non-numeric USER (e.g. `node`) then fail kubelet verification (`CreateContainerConfigError`) — fix by setting numeric `runAsUser` in values | Before adding a key to a Helm values block, grep the block for an existing key and MERGE into it; validate rendered output (`helm template` + check the final image tag), not just YAML syntax. For mutate-injected `runAsNonRoot`, pair it with explicit `runAsUser: <uid>` when the image USER is non-numeric. (f1d7333) |

### GitOps/ArgoCD Mistakes
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-08-09 | The 2026-08-05 three-part Gitea-registry lesson (below) **re-manifested wholesale on Kakao four days after being "fixed"**, because all three parts lived in repo HEAD and none had reached this cluster: the Helm package registry was EMPTY (`entries: {}`), `argocd-tls-certs-cm` had zero keys, the `narwhal-charts-repo` Secret did not exist, and the live `gitea-git-bypass` regex lacked `^/api/packages/`. Yet every app read **Synced** for 4 days and prometheus-stack had deployed 86.2.3 successfully — the ArgoCD repo-server chart cache answered for the empty registry, exactly like the registry blob-cache incident of 08-06. The first hard refresh tore the mask off: `ComparisonError: chart not found`, then `x509: unknown authority`, then `gzip: invalid header` (the OIDC login page served where a tarball should be), one hidden layer per fix. Root of the CA half: `13-argocd.sh` guards CA population with `\|\| true` when the wildcard cert is not yet issued — on Kakao it was not, the block silently skipped, and nothing ever re-ran it | Republished the 27 bundled charts, populated the cm (both hostname keys) and repo Secret per `13-argocd.sh`, pushed the bypass-regex file to gitea. Three discriminators worth keeping: (1) **`Synced` means "matches the in-cluster gitea copy", never "matches repo HEAD"** — the two drift independently per cluster, so verify a fix reached a given cluster by reading its live object, not by repo grep; (2) a cache in front of a registry makes emptiness invisible until refresh — after any registry repair, hard-refresh one chart-sourced app as the acceptance test (the 08-05 row's own advice, not followed on this cluster); (3) an install-time `\|\| true` guard on a dependency that "is not ready yet" needs a later step that actually re-runs it, or it is a silent permanent skip. Sequenced errors are also a signal: each fix revealing a *different* error is progress through stacked causes, not a failing fix |
| 2026-08-09 | UI in portal compliance tab reported "waiting for Trivy Operator scan" permanently for KISA framework. Assumed trivy-operator scan was pending, but no KISA ClusterComplianceReport CRD resource existed on the cluster | Created `ClusterComplianceReport` for KISA (`gitops/resources/kisa-compliance.yaml`) mapping KISA controls to real Trivy check IDs (`AVD-KCV-*`, `AVD-KSV-*`) and wired via ArgoCD (`gitops/charts/narwhal-apps/templates/kisa-compliance.yaml`). Discriminator: A UI saying "waiting for scan" can mean the underlying custom resource was never created at all — list CRDs (`kubectl get clustercompliancereports`) to distinguish uncreated resources from delayed scan jobs |
| 2026-07-31 → 08-04 | **`accounts.narwhal-portal` did not exist when `13-2-narwhal-portal-bindings.sh` asked ArgoCD for a token, so the failure was deterministic — and it was twice misread as a timeout.** The token endpoint answered `account 'narwhal-portal' does not exist`; admin login succeeded, which is why the retry loop looked like it was almost working. The account's only declaration lives in `gitops/charts/narwhal-platform/templates/argocd-config.yaml`, which ArgoCD applies at `14-gitops-bootstrap.sh` — one step AFTER the script that needs it — while 13-2's own header comment claimed 13-argocd.sh created it. The script then wrote `REPLACE_ME__argocd_token` into narwhal-portal-secrets, the portal caught the 401, logged `Connection failed, returning empty` and rendered an empty widget, so the cluster read as healthy | 13-argocd.sh now writes `accounts.narwhal-portal: apiKey,login` into argocd-cm with the value the chart uses, so the later GitOps sync is a no-op. **The diagnostic lesson is the expensive one:** it "worked" whenever anyone re-ran 13-2 by hand, because by then GitOps had caught up — which made it look like a timing problem and got it "fixed" twice by waiting longer (12×10s → 30×10s). Read the API's own error before tuning a timeout; `{"error":"account ... does not exist"}` was there the whole time and was being discarded. What did survive from those attempts is worth keeping: verify the token authenticates (200) before storing it, and store nothing if it does not — a placeholder and an absent key break the portal identically, and only one lies about it |
| 2026-07-07 | Portal deployed via a `:latest` image tag in the GitOps manifest — ArgoCD compares the rendered manifest string, so a re-published `:latest` is an IDENTICAL manifest → **no diff detected → no sync → stale image runs** until a manual `rollout restart`; selfHeal never fires either. (D-portal-ghcr) | Pin an immutable SemVer tag in GitOps (`ghcr.io/dasomel/narwhal-portal:1.0.0`). Upgrade = bump the tag → ArgoCD sees the diff → auto-syncs. Reserve `:latest` for non-GitOps flows. Portal now pulls the pinned public GHCR image; in-cluster Kaniko build demoted to optional dev tool (`docs/common/developer-kaniko-builds.md`) |
| 2026-07-05 | `scripts/gitops/push-to-gitea.sh` uses `cp -r` (mirror or per-path) into a fresh Gitea clone — this only adds/overwrites files, it never deletes a file that no longer exists in the local `gitops/` tree. Renaming/removing a gitops Application file (e.g. `promtail.yaml` -> `k8s-monitoring.yaml` for the Alloy migration) and running the script as-is would leave the stale old file committed in Gitea alongside the new one -> two conflicting ArgoCD Applications | For a rename/delete, do the Gitea clone/push manually with an explicit `git rm` instead of relying on the script's plain `cp -r` |
| 2026-07-10 | Ran `git push -f gitea main` from the narwhal repo DIRECTLY to the in-cluster `narwhal-gitops` Gitea repo — the two have DIFFERENT tree structures (gitea root = CONTENTS of `gitops/`), so the force-push replaced Gitea main with the entire narwhal repo tree (Vagrantfile, scripts/, docs/ …); a later `push-to-gitea.sh` mirror superimposed the correct root so ArgoCD kept working, masking the pollution. Also `git remote set-url` embedded the gitea-admin PASSWORD in plaintext in `.git/config` | NEVER push the narwhal repo directly to the gitea remote — always go through `scripts/gitops/push-to-gitea.sh` (it clones fresh and maps `gitops/` -> repo root). Cleanup was a clone + `git rm -r` of the non-gitops paths + normal push. Never embed credentials in a git remote URL; the script reads the password from the cluster secret per-run |
| - | values file path typo | `valueFiles` paths are relative to repoURL |
| - | targetRevision format error | Chart version is `"1.0.0"` (string), Git ref is `HEAD` |
| 2026-02-14 | app-of-apps repoURL uses `https://` -> Gitea is HTTP only | Use `http://gitea-http.gitea.svc.cluster.local:3000/...` |
| 2026-02-14 | Harbor gitops YAML missing ARM64 image overrides | Must specify all components: `ghcr.io/dasomel/goharbor/*:latest` |
| 2026-02-14 | SeaweedFS chart version 4.0.410 -> Docker Hub image 4.10 doesn't exist | Verify appVersion=image tag exists before using (4.0.407->4.07) |
| 2026-02-14 | ArgoCD repo-server GitHub Pages IPv6 connection failure | VM does not support IPv6, ArgoCD fails when attempting IPv6 |
| 2026-02-14 | ArgoCD managed resource Helm upgrade conflict | Patch directly with `kubectl set image`, use kubectl instead of Helm |
| 2026-02-15 | Headlamp v0.40.0 has no `-oidc-skip-issuer-tls-verify` flag | Mount CA cert to `/etc/ssl/certs/` with subPath, do not use `SSL_CERT_FILE` |
| 2026-02-15 | Grafana `assertNoLeakedSecrets` chart validation failure | `grafana.assertNoLeakedSecrets: false` required |
| 2026-02-15 | Prometheus Helm release name mismatch (`prometheus` vs `prometheus-stack`) | Must match ArgoCD app name for HTTPRoute/ConfigMap to work correctly |
| 2026-02-15 | Gitea OIDC source add fails with self-signed cert verification | Mount CA cert in Gitea container before running `add-oauth` |
| 2026-02-24 | ArgoCD installed in non-default namespace (devtools) ClusterRoleBinding Subject mismatch | Upstream install.yaml always sets Subject namespace to `argocd`. After installation, `kubectl patch clusterrolebinding argocd-application-controller argocd-applicationset-controller argocd-server` to update namespace to actual installation namespace |
| 2026-02-24 | Istio ambient namespace ArgoCD health check ports (8082/8084/9001) blocked by ztunnel | ztunnel intercepts all inbound traffic in ambient namespace. kubelet probe (plain HTTP) conflicts with ztunnel expecting mTLS -> CrashLoopBackOff. Add `istio.io/dataplane-mode: none` label to pod template to exclude from ambient. `traffic.sidecar.istio.io/excludeInboundPorts` annotation does not work in ambient mode |
| 2026-02-26 | ArgoCD selfHeal reverts kubectl apply changes | ArgoCD managed resources revert to Gitea state even when modified with kubectl. Must push to Gitea repo for persistence |
| 2026-02-26 | Gitea headless service (ClusterIP: None) -> git clone fails | DNS resolution fails with headless Gitea service. Use pod IP directly: `kubectl get pod -l app.kubernetes.io/name=gitea -o jsonpath='{.items[0].status.podIP}'` |
| 2026-06-11 | APISIX 3.16 has NO `kubernetes` secret manager (only vault/aws/gcp) — `$secret://kubernetes/k8s-1/...` ApisixRoute refs can never resolve and `secret_providers` in config.yaml is not a real APISIX option. IC meanwhile DID sync ExternalName-backed routes, creating broken duplicates that shadowed the working manual admin-API routes → velero-ui (and 5 others) `unauthorized_client` → callback 500 | Inject OIDC secrets via apisix chart `extraEnvVars` (secretKeyRef) + reference `$env://VAR` in ApisixRoute; delete legacy manual duplicate routes from etcd via admin API |

### Vagrant/Infrastructure Mistakes
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-08-05 | Migrating those 21 sources to Gitea's Helm registry failed twice more, each time looking like the previous fix had not worked. (1) Gitea publishes `index.yaml` with **absolute** download URLs built from `ROOT_URL`, so ArgoCD — pointed at the in-cluster service — was redirected out to `https://gitea.<domain>/api/packages/…` and met a Keycloak redirect, because that host is behind the APISIX OIDC plugin. A registry is a machine endpoint; a machine cannot complete a browser flow. (2) With the route fixed it still failed on `x509: certificate signed by unknown authority` **even though the CA was already in `argocd-tls-certs-cm`** — repo-server only passes `--ca-file` to the `helm pull` subprocess for repositories it has been *told about* | Three parts, and all three are needed: add `^/api/packages/` to the `gitea-git-bypass` ApisixRoute; put the CA in `argocd-tls-certs-cm` under **both** hostnames (the Application names the service, the redirect lands on the ingress, and the lookup is by configured host); and declare the repo as an `argocd.argoproj.io/secret-type: repository` Secret — without it the ConfigMap is populated and unused, which is the most misleading state of the three. Also `12-gitea.sh` now publishes the bundled `.tgz`s so a clean install populates the registry before the app-of-apps comes up. Verified the way the row below says to: hard refresh on an isolated cluster, 34/34 back to Synced |
| 2026-08-05 | The install went fully offline; **the GitOps layer did not**, and `34/34 Synced Healthy` hid it. 18 ArgoCD `repoURL`s in `gitops/` name public chart repositories (`charts.jetstack.io`, `metallb.github.io`, `github.com/otwld/velero-ui`, …), so every refresh/resync/selfHeal needs the internet. It read as fully Synced because those syncs happened while a default route was still present — the moment isolation was made durable, a forced hard refresh turned harbor, kyverno, metallb, openbao and velero-ui all to `Unknown` with `failed to list refs: ... lookup github.com`. Running workloads were unaffected (161/161), which is exactly why this can sit unnoticed | Never read `Synced` as evidence of anything offline: it describes the last successful comparison, not the ability to make one now. **Force a hard refresh and re-read** — that is the only check that distinguishes "syncs offline" from "synced while online once". Found only because the isolation fix in the row below made the gap persist; with the route silently restored by DHCP, this would have passed every test forever. Fix direction (not yet done): serve the bundled chart `.tgz`s from the in-cluster Harbor as an OCI Helm repo and rewrite the sources, since the charts are already in the bundle. `regression-check` R31 counts the remaining public sources so the number cannot grow while that migration is pending |
| 2026-08-05 | **The once-per-run "SSH drop" was never a drop.** VMware's NAT DHCP gives the same address to two different MACs, and the VMware provider derives each machine's forwarded SSH port *from that address* — so two machines share both, and `vagrant ssh <a>` reaches `<b>`, whose key does not match. Measured live with distinct MACs: master-2 + worker-3 on `172.16.221.135` / port 2204, master-1 + worker-2 on `.133` / port 2203. This is why every in-VM investigation came up empty on 2026-08-04 — nothing happened inside the VM. It also explains the randomness: it depends on which machine boots into the collision, not on what the provisioner was doing. The expensive part was not the failed provision but the **misreport**: `up.sh`'s readiness probe goes through `vagrant ssh master-1`, so it announced `0/6 nodes Ready` for a cluster with 5 Ready nodes and "recovered" a healthy master-1 with a reload | Three changes in `up.sh`. (1) `master1_exec()` reaches master-1 on its static host-only address `192.168.56.10` with that machine's own key — unique per VM, never shared, so the readiness gate cannot be fooled. (2) `nat_ip_duplicates()` runs at the top of every attempt and names the colliding pair, turning a mystery auth failure into a stated cause. (3) `repair_nat_duplicates()` reloads the worker of each pair — a **reload** is what actually moves a guest; the in-guest `networkctl renew` the 2026-07-29 row recommends does not, the server just re-offers the same lease to the same MAC (measured 40/40 no-move). Discriminator: distinct MACs holding one IP means the DHCP server, not the guests; read it with `vmrun getGuestIPAddress` AND from inside the guest, never from `vagrant ssh-config` (which reports the forwarded port and so inherits the confusion being diagnosed) |
| 2026-08-05 | `ip route del default` was the whole of "enforce isolation", and it does not hold: DHCP renewal makes systemd-networkd install the gateway again. Measured on the finished cluster — all six nodes had their default route back while the install log showed it removed on each. Provisioning itself did run isolated (that is when every leak surfaced), so the check looked like it worked; what it could not do is keep the delivered cluster offline, which means a later "still works offline?" test would be testing nothing | A networkd drop-in (`[DHCPv4] UseGateway=false / UseRoutes=false`) next to the file that manages the link, then delete the route. Resolve that file with `networkctl status <if>` — do NOT glob for `*<iface>*.network`: netplan names the rendered file after the netplan id (`10-netplan-main_if.network` for `enp2s0` on this box), so the glob matched nothing, created no drop-in, and reported success. Verified against a forced `networkctl renew`: no route returns, NAT address and host-only address both intact. Second lesson in the same fix — a removal that a daemon can undo is a pause, not a policy. **Two Kakao corrections (2026-08-06):** the fix landed after the Kakao cluster was built, so those six nodes had their routes back too (measured: `proto dhcp` default + internet 301) — a durable-isolation fix protects only clusters built after it, existing ones need it applied by hand. And on Kakao the drop-in must be `UseGateway=false` ONLY: the lease also delivers the metadata/NTP endpoints as classless routes (`169.254.169.254 via 172.17.0.4`), and `UseRoutes=false` discards those with the gateway, breaking cloud-init on the next boot. On the Vagrant NAT link the lease carries only the gateway, so dropping `UseRoutes` there never did anything — check `ip route \| grep "proto dhcp"` before deciding what a lease can be stripped of |
| 2026-08-06 | The static regression suite died with exit 1 and **zero FAIL lines** the moment cd64ff4 removed the last public repoURL: R34's counting pipeline ends in `grep -v \| sort \| wc`, and with nothing left to count `grep -v` emits nothing and exits 1, which pipefail turns into death mid-suite. The check crashed precisely at its own success case, and it could never have been caught earlier — the failure needs the watched problem to be *fixed* first. The known non-matching-grep rule, violated in a check written the same day the rule was sharpened | `\|\| true` on the pipeline, empty→0 guard after. Discriminator worth keeping: a check suite exiting non-zero with no FAIL output has **crashed**, not failed — read the last check that printed and look at the next one in the file. And when writing a check that counts occurrences of a problem, test it against the zero case before shipping: that is the case it exists to reach |
| 2026-08-05 | Two Helm **hook/Job** images were absent from the airgap bundle: `cert-manager-startupapicheck` (blocked cert-manager's post-install and failed 08-1 networking) and `kube-webhook-certgen` (kube-prometheus-stack's admission patch Job). Both are invisible to `01-generate-image-list.sh --live`, which enumerates what a *running* cluster uses — a hook Job exists only during install and is gone before any scan. The controller/webhook/cainjector images of the very same chart were all present, which is what makes the gap look impossible at first | Add hook images explicitly; longer term the generator needs `helm template --include-crds` over the bundled charts rather than a live scan, because no amount of scanning a healthy cluster will ever show them. Catching them cheaply: watch the live cluster for `ImagePullBackOff`/`ErrImagePull` during Phase 2 and top the mirror up as they appear — that found both in minutes, where reading charts would have been guesswork. Not every pull failure is a list gap: `kyverno/cleanup-controller` and `reports-controller` were in `images.txt` AND in the registry catalog, yet no node could pull them until re-pushed with skopeo; cause undetermined (neighbouring kyverno images stored the correct arm64, so it was not architecture) |
| 2026-08-05 | `05-nfs-quota-agent.sh` untarred the bundled chart and then failed with `Error: path "/tmp/nfs-quota-agent-chart" not found` — after the extraction had reported nothing wrong. The tarball's top-level directory is `nfs-quota-agent/`; the `-chart` suffix only ever matched the GitHub tarball the script used to download. Swapping the source (public URL → `chart` from the bundle) preserved the command but not the layout, and `tar` exits 0 either way | `tar xzf "$(chart X)" -C "${CHART_DIR}" --strip-components=1` so the destination is named by the script rather than by the archive. Discriminator: a helm "path not found" immediately after a clean extraction means the directory name, not a missing file — `tar tzf` the bundle and compare. General form: when a fetch is replaced by a bundle lookup, re-check what the artifact *unpacks into*, not just that the command succeeds; this is the same shape as the bin/manifests mount gap in the row below |
| 2026-08-05 | An offline install reached the CNI step and stopped at `ERROR: 'helm' is not in /home/vagrant/bin`. The binary was in the bundle the whole time — `lib-charts.sh` resolves three directories (`NARWHAL_CHART_DIR`, `NARWHAL_BIN_DIR`, `NARWHAL_MANIFEST_DIR`), the Kakao path stages all three, and the Vagrantfile mounted only `charts` and `apt`. The bundle-building work and the mounting work were done in different passes, so nothing compared the two lists | Mount `bin/` and `manifests/` alongside `charts/`; the three belong together and adding one without the others is what produced the gap. Verified by remounting a live node rather than by re-running the install: `-x` survives the HGFS mount and `/home/vagrant/bin/helm version` prints v4.2.1. Cheap general check before any rebuild — diff what the scripts reference (`chart`/`manifest`/`install_bin` call sites) against what the bundle actually contains; it caught nothing else here, which is the point |
| 2026-08-05 | `kubeadm init` succeeded, then `kubeadm init phase upload-certs` died with `error: cannot use "0.0.0.0" as the bind address for the API Server`. The phase was invoked without `--config`, so kubeadm built a default InitConfiguration with an empty advertiseAddress and inferred one from the default route — which the airgap step had deliberately removed. Online it never failed *and never agreed*: the inference picked the NAT interface while `kubeadm init` had been given `MASTER1_IP`, and nothing reported the mismatch | Pass `--config=/tmp/kubeadm-config.yaml` to the phase so it reads the same config the cluster was initialized with. A/B on the live node: without it the error, with it a certificate key. General rule: any `kubeadm` subcommand run without `--config` re-derives its address from routing — `02-join-control-plane.sh` already appends `--apiserver-advertise-address` for exactly this reason. Related, same run: `kubernetesVersion: stable` made kubeadm fetch `dl.k8s.io/release/stable.txt`; it fails safe to the binary's own version, so offline it was only a warning, but the control-plane version was being decided by a network lookup — now pinned to `v${K8S_PATCH_VERSION}` |
| 2026-08-05 | With containerd mirrors configured and every other pull served offline, `00-kube-vip.sh` still died with `dial tcp 20.200.245.241:443: network is unreachable`. `/etc/containerd/certs.d` is read by containerd's **CRI plugin**, so kubelet and crictl inherit the mirror for free — but `ctr` is a direct client of the content store and resolves images itself, ignoring certs.d entirely. It was the one image pull in the provisioning path that does not go through CRI, and therefore the only one still reaching the internet | `sudo ctr image pull --hosts-dir /etc/containerd/certs.d <ref>`. Proven by A/B on the same isolated node: without the flag `network is unreachable`, with it `Completed pull ... elapsed: 0.7 s` from the mirror. Discriminator: mirror works for everything *except* one step → look for a non-CRI image client (`ctr`, `nerdctl`, kaniko, skopeo), not for a missing hosts.toml. regression-check R30 now fails any `ctr ... pull` without `--hosts-dir` |
| 2026-08-05 | The apt bundle carried only what `01-prerequisites.sh` installs, because that is the script the airgap work started from. dnsmasq (04-addons) and nfs-kernel-server/quota/quotatool (05-nfs-quota-agent) were absent, each one a `has no installation candidate` waiting 25 minutes into an offline run. The list was also duplicated inside 07 — once for the download, once for the dependency closure — so fixing one and not the other was the natural next mistake | One `AIRGAP_APT_PACKAGES` used by both, plus a coverage check in 07 that scans the provisioning scripts for what they actually `apt-get` and refuses to build a short bundle. The scanner is where the real care went: it must match installs guarded by `dpkg -s x \|\| ...` (not at line start), join backslash continuations, and **not** strip flags with `s/-[a-z]+//g` — that turns `nfs-common` into `nfs` and the check passes while the bundle is still wrong. Bundle went 123 → 140 .debs |
| 2026-08-05 | The first AIRGAP=1 clean install died on every node at the same line: `E: Failed to fetch file:/home/vagrant/apt/./Packages  File not found`. The index was not missing — `Packages.gz` was there, 0644, and root could read it. apt drops privileges to the `_apt` user before fetching, and `/home/vagrant` is `0750 vagrant:vagrant`, so `_apt` could not traverse into the mount. apt reports that as a missing **uncompressed** `Packages` (it silently gives up on `.gz` first), which points at bundle generation instead of at directory permissions | Mount the apt bundle at `/srv/airgap/apt` (0755 root:root all the way down), not under `/home/vagrant`. Vagrant's own copy at `/mnt/vagrant-mounts` is `0700` and fails the same way, so re-pointing at it is not an escape. Discriminator: `sudo -u _apt test -r <dir>/Packages.gz` — root-can/`_apt`-cannot is this bug, and `namei -m` names the offending directory. 01-prerequisites.sh now runs that check before writing the sources line, so the next occurrence says "not readable by the _apt user" instead of "File not found". Only `apt/` moved; `charts/` stays under `/home/vagrant` because helm runs as root and `lib-charts.sh` resolves it there |
| 2026-08-02 | `kubectl get nodes` returned `host-172-17-0-*` to a session that read them as the local Vagrant cluster's nodes. Nothing had gone wrong with the cluster: another session had run `set-config-kakao.sh` hours earlier, and both set-config scripts end with `kubectl config use-context`, which rewrites current-context in the **shared** `~/.kube/config` for every shell on the machine. Each script printed only its destination, so a move off another cluster was invisible unless you already knew where you had been — and the two contexts differ by one suffix. Sibling of the 2026-07-29 `set-config.sh` row below: same script, but that one exits silently while this one succeeds silently | Print both ends (`switched: <from> -> <to>`) and warn when the previous context pointed at a different API server. Discriminate on the **server**, never the context name: the first attempt tested whether the old name started with `CLUSTER_NAME`, and `narwhal-kakao` starts with `narwhal`, so the single move that matters read as a routine auth change — verified against the live kubeconfig, the prefix test stays silent exactly where the server test fires. For readers: `kubectl --context <name>` sidesteps the shared setting entirely, and is worth using in any script or doc that must not care what another session did |
| 2026-07-29 | "Cluster unreachable" had three unrelated causes stacked, and each looked like the others. (1) All six VMs were halted. (2) macOS Local Network Privacy was blocking the terminal app, so every unicast to `192.168.56.0/24` died while ARP still resolved and the internet still worked — `route get` pointed at the right bridge with the right MAC, and a `tcpdump` on the VM's `enp26s0` saw *zero* of four pings sent from the host. (3) VMware's NAT DHCP handed the same lease to two guests (master-1 + worker-2 both on `172.16.221.133`, master-2 + worker-3 both on `.135`), so `vagrant ssh master-1` authenticated master-1's key against worker-2 and got `Permission denied`, then `No route to host` once the two started fighting over ARP | Localize before theorizing: ping the host's own interface (`192.168.56.1`), a VM, the LAN router, and a public IP. Own-interface + internet OK while *every* LAN target fails is macOS Local Network Privacy, not the cluster — enable the terminal app under System Settings > Privacy & Security > Local Network. For the duplicate lease, verify uniqueness with `vmrun getGuestIPAddress` per machine, not `vagrant ssh-config`. **Superseded on 2026-08-05 in two ways** (see the row dated that day): the in-guest `networkctl` renew recommended here does NOT move a guest — the server re-offers the same lease to the same MAC — a `vagrant reload` does; and cause (3) turned out to be far more than a one-off, it is the long-unexplained per-run SSH failure |
| 2026-07-29 | `set-config.sh` printed its banner and exited silently against halted VMs — twice, and both times it read as success. `CONFIG=$(vagrant ssh master-1 -c "cat ~/.kube/config" 2>/dev/null)` swallowed the reason and `set -e` killed the script before `kubectl config use-context`, leaving the *previous* context active. It also reached master-1 over the NAT network, which proves nothing about whether kubectl can reach the API server | Never `2>/dev/null` a command whose failure ends the script. Fetch over the same network the resulting kubeconfig points at (host-only `MASTER_IP`), so a successful fetch proves the context is usable, and preflight both "is the VM running" and "does the API server answer from *this* machine" before writing any config |
| 2026-07-06 | Assumed `ghcr.io/dasomel/goharbor/*:latest` tracked Harbor v2.15.2 (the source release that prompted a chart bump) solely because `docker manifest inspect` showed multi-arch (amd64+arm64) support — that only proves the tag has ARM64 builds, it says nothing about which app version is behind it. Live check via `curl .../api/v2.0/systeminfo` showed the actual running `harbor_version: v2.15.0-f585d00b` — the custom registry hadn't rebuilt from 2.15.2 source yet | For "is version X actually running" questions, query the app's own version-reporting endpoint/API (Harbor: `/api/v2.0/systeminfo`) — never infer app version from unrelated signals like manifest architecture support |
| 2026-07-05 | `08-4-storage.sh`'s SeaweedFS S3 bucket creation was fire-and-forget: `kubectl wait --for=condition=Ready pod -l ...=filer` only confirms the container passed its own readiness probe, not that the embedded S3 API is accepting requests yet. Observed live: the single bucket-create pass silently no-op'd for `tempo`/`velero`/`loki` (only `cnpg-backup` landed, apparently created later by CNPG's own first write) while the verify loop printed WARNs and the script still exited 0 (`\|\| true` everywhere) → Tempo crashlooped ("bucket does not exist"), Loki logged continuous `NoSuchBucket` errors on every index/ruler sync, Velero's backup-location went Unavailable | Retry each bucket's create+verify individually (5 attempts, 10s apart) and `exit 1` if one never lands instead of swallowing the failure — a stopped install here is far cheaper than three components silently degrading downstream |
| 2026-01-29 | Vagrant Cloud 401 error | Use `VAGRANT_CLOUD_TOKEN=$(hcp auth print-access-token)` when HCP token required |
| 2026-01-29 | charts.keycloak.org URL 404 | Use official Keycloak Operator (`keycloak-k8s-resources` GitHub) |
| 2026-01-29 | Bitnami image not found | Use official images or Operators (CloudNative-PG, etc.) due to Bitnami commercialization |
| 2026-01-29 | kubeadm init fails on vagrant provision re-run | Add logic to check if cluster already initialized, or run individual scripts directly |
| 2026-01-29 | Keycloak DB connection fails with FQDN | Use short service name in same namespace (`keycloak-db-rw` not `keycloak-db-rw.keycloak.svc.cluster.local`) |
| 2026-01-29 | Gitea Helm chart valkey FQDN issue | `gitea-init` secret script uses FQDN, needs patching. Set `valkey.enabled=true`, `valkey-cluster.enabled=false` in script |
| 2026-01-29 | Keycloak 2 replicas resource shortage | Use `instances: 1` in test environment, HA only for production |
| 2026-01-30 | Cluster DNS not accessible from API server | Expose OIDC issuer URL via NodePort (`http://NODE_IP:30080/realms/kubernetes`) |
| 2026-01-31 | OpenBao v2.4.4 no ARM64 image | No ARM64 at quay.io/openbao/openbao, use tag `2.2.0` (default registry) |
| 2026-01-31 | Velero bitnami/kubectl image not found | Bitnami banned, use `docker.io/alpine/k8s` (includes shell, ARM64 support) |
| 2026-01-31 | Pod eviction due to disk pressure | Add `node.kubernetes.io/disk-pressure:NoSchedule` to `tolerations` |
| 2026-01-31 | GitHub Actions get-changed-files doesn't support workflow_dispatch | Add `if: github.event_name != 'workflow_dispatch'` condition |
| 2026-01-31 | CNPG cluster replica WAL archiving failure | Delete problematic replica PVC and wait for auto-recreation, reduce instance count to stabilize |
| 2026-02-10 | MetalLB Helm upgrade CRD field manager conflict | Initial install is fine, re-upgrade has CRD caBundle conflict. Pods function normally. **This "harmless" verdict did not hold** — see 2026-08-04 above, where the same field-ownership conflict took out three components' re-install path under Helm 4 and blocked Phase 2 entirely. Apply `--force-conflicts` |
| 2026-02-10 | Harbor ARM64 `exec format error` | Use `ghcr.io/dasomel/goharbor/*:latest`, official images are AMD64 only |
| 2026-02-10 | OAuth2 Proxy cookie_secret 34-byte error | `openssl rand -base64 32` produces 44 bytes, use `openssl rand -hex 16` for 32 bytes |
| 2026-02-10 | Velero CRD job kubectl missing `/bin/sh` | `registry.k8s.io/kubectl` is distroless, use `docker.io/alpine/k8s` |
| 2026-02-10 | VERSIONS.md out of sync with actual deployed versions | Must sync VERSIONS.md based on chart versions pinned in scripts |
| 2026-02-11 | CNPG DB password hardcoding mismatch | Must query actual password from CNPG Secret (`harbor-db-credentials`) |
| 2026-02-11 | Loki 6.52.0 bucketNames required | Must specify `loki.storage.bucketNames.{chunks,ruler,admin}` |
| 2026-02-11 | Traefik 39.0.0 HTTPS certificateRefs required | TLS certificate reference required for Gateway HTTPS listener |
| 2026-02-11 | Traefik Helm --set port type error | Use values file instead of `--set` (float64 vs int64) |
| 2026-02-11 | ArgoCD managed resource Helm reinstall conflict | Delete existing resources (SA, IngressClass, GatewayClass) before reinstall |
| 2026-02-28 | Keycloak HTTPRoute + Operator Ingress both active -> 502 | Keycloak Operator manages Ingress automatically, do not create HTTPRoute. HTTPRoute + Ingress coexisting on same Host causes Traefik WRR null backend selection |
| 2026-02-28 | Keycloak pod restart loses `istio.io/dataplane-mode: none` label | Must set permanently in Keycloak CR `spec.unsupported.podTemplate.metadata.labels`. Direct pod label addition is lost on restart |
| 2026-06-17 | `up.sh` Phase 2 call used `\|\| true` + final `echo`-only exit, so clean-install failures (SSH key race, mid-provision disconnect) silently exited 0 | Capture Phase 2 provision exit code; on non-zero, recover master-1 SSH and retry once; exit 1 on failure so the caller sees a real error |
| 2026-06-17 | `vagrant provision` over VMware SSH key race kills only the 06-phase2 orchestrator via SIGHUP; sub-scripts (08-1 onward) become orphans and Phase 2 stalls mid-way; yet master-1 is k8s-Ready so the main retry loop skips it | `up.sh` now checks `master1_ssh_ok` before Phase 2 and calls `recover_master1_ssh` (`vagrant reload master-1` + 40×15s poll) on failure; 3-master HA keeps the control plane quorate during the single-node reboot; phase2-platform is idempotent so the retry completes safely |
| 2026-06-17 | Cluster nodes' systemd-resolved leaked `harbor.local.narwhal.internal` to the NAT interface (public DNS) → resolved to a public AWS IP → image pull failed with `tls: unrecognized name` | Add `192.168.56.200 harbor.local.narwhal.internal` (APISIX LB VIP) to each node's `/etc/hosts` in `01-prerequisites.sh`, and enable containerd `config_path` + a `certs.d/harbor.local.narwhal.internal/hosts.toml` with `skip_verify = true` in `02-containerd.sh` (internal registry behind APISIX — no node CA trust needed) |
| 2026-06-30 | containerd 2.2.2 (Ubuntu 26.04) emits `config_path = ''` (single-quoted) under plugin `io.containerd.cri.v1.images`; the old grep/sed in `02-containerd.sh` matched neither the quote style nor the 2.x namespace → config_path stayed empty on workers → certs.d ignored → Harbor (private CA) image pull TLS fail → portal ImagePullBackOff (D3) | Replace grep/sed with a `python3 re.sub` that handles both quote styles (`''`/`""`) and both plugin namespaces (`cri.v1.images` / `grpc.v1.cri`); exits 1 loudly if no registry section. (2e49e5e) |
| 2026-06-30 | cilium-operator hits transient `Unauthorized` from the apiserver during HA bring-up (SA token not yet issued) → "Failed to start hive" exits → containerd keeps the container name reserved → operator stuck CreateContainerError even after apiserver stabilises → Cilium degrades cluster-wide → metrics-server/pods can't get sandbox → `07-cnpg` times out (D6) | Add `cilium_health_gate` at start of `06-phase2-start.sh`: poll up to 300s for all cilium agents+operator Ready; at 150s purge Unknown/CrashLoop/CreateContainerError cilium pods so they reschedule (plain pod-delete clears the wedge); exit 1 only if it never converges. (273dcbc) |
| 2026-06-30 | Vagrantfile fired phase2-platform from a worker-3 `trigger.after :up`, but under `up.sh`'s per-VM resilient bring-up worker join order isn't guaranteed → trigger fired before all nodes stably Ready → nodes flapped (6→0→6) during Phase 2 → `08-1` kubectl ops failed (D7) | Remove the auto-trigger; make `scripts/up.sh` the sole Phase 2 driver (it polls all nodes Ready then runs phase2-platform once). Bare `vagrant up` no longer auto-runs Phase 2 by design. (5be1028) |
| 2026-06-30 | VMware SSH key-replacement race SIGHUPs the Phase 2 orchestrator mid-run (sub-scripts orphan, Phase 2 stalls partway — e.g. monitoring/storage ns never created); `up.sh`'s single retry was insufficient when the race recurred (D11) | `up.sh` Phase 2 is now a bounded retry loop (`PHASE2_MAX_ATTEMPTS=3`): recover_master1_ssh before each attempt, run phase2-platform (idempotent — resumes where it stalled), gate on phase2_complete, exit 1 only after all attempts. (a426b73) |
| 2026-06-30 | (narwhal-portal repo) in-cluster Kaniko build OOMKilled at 4Gi (Next.js standalone build) because `--compressed-caching=true` kept uncompressed+compressed layers in RAM; also 502 when pushing to a mid-restart Harbor (D8) | Set `--compressed-caching=false` (keep 4Gi limit); add a Harbor readiness gate (`kubectl rollout status` harbor-core+harbor-registry) before the Kaniko Job in `kaniko-build.sh`. narwhal-portal commit 8fe4159. |
| 2026-07-01 | A host reboot / `vagrant up` resume ran NO recovery (the Phase-2 `cilium_health_gate` only fires during provisioning), so a cold boot left a containerd stale-container wedge, Cilium/Istio `Unknown` ghosts, and platform CrashLoops — ingress dead until manual containerd restarts + pod cleanup | Two in-guest systemd oneshot units installed at provision, fired every boot: `narwhal-boot-heal.service` (all nodes — detect wedge, restart containerd+kubelet) + `narwhal-cluster-heal.service` (master-1 — restart not-Ready CNI, delete `Unknown` ghosts, kick CrashLoops, poll convergence). Validated via `vagrant reload`: converged to 0 non-running, ingress 307, no manual intervention. See `docs/vagrant/reboot-recovery.md`. (b91091e) |
| 2026-07-01 | After reboot, static pods (etcd/apiserver/scheduler/controller-manager) showed `0/1 Running` with `<invalid>` restart ages: no working time sync (earlier "chrony not found") meant the kubelet started before chrony's `makestep` fixed the clock, so static-pod startTimes were recorded against a skewed clock → readiness never reconciled | Install chrony reliably; add `narwhal-clock-sync.service` (`chronyc waitsync`) + a `kubelet.service.d` drop-in so kubelet only starts once the clock is synced. To clear it on an already-skewed node: sync the clock then restart kubelet. (070149d) |
| 2026-07-01 | containerd 2.x `systemctl restart/stop containerd` leaves the **shim** alive; its child (the real container process) reparents to PID 1 and keeps holding ports/sockets/bolt-DB entries → `CreateContainerError` "name reserved" persists even after a containerd restart (bit deeper masters/openbao/etcd after reboot) | A plain containerd restart clears the common wedge (boot-heal), but a deep wedge needs: kill the reparented child PIDs, remove the CRI state dir `/var/lib/containerd/io.containerd.grpc.v1.cri/containers/`, AND `ctr -n k8s.io containers rm <id>` to drop the bolt-DB name reservation, then restart kubelet. |
| 2026-07-07 | Fresh clean install: ALL harbor pods CrashLoopBackOff with `exec /harbor/entrypoint.sh: exec format error` on arm64 nodes, even though `ghcr.io/dasomel/goharbor/*:latest` is a valid multi-arch image (the registry's arm64 variant is a genuine aarch64 binary, confirmed via `crane export`). Root cause: the `dasomel/ubuntu-26.04-xfs` Vagrant box bakes STALE amd64 `:latest` harbor layers into its containerd content store; a **mutable** tag is never re-fetched (a re-pull transfers only the ~16 KB manifest and reuses the baked amd64 layers — even `ctr pull --platform arm64` or pull-by-digest just fetch the manifest), so arm64 nodes execute amd64 binaries. Directly contradicts the older 2026-02-10 lesson ("use `:latest`") — for a baked box `:latest` is the trap. | **Pin harbor images to an IMMUTABLE version tag** in BOTH `08-5-registry.sh` (helm `--set`) and `harbor.yaml`: `:v2.15.1` for core/jobservice/registry-photon/registryctl/portal/nginx/redis-photon. A different tag has different layer digests absent from the stale bake → forces a real arm64 layer pull (verified `ctr run :v2.15.1 uname -m` → aarch64). **harbor-exporter EXCEPTION**: `ghcr.io/dasomel/goharbor/harbor-exporter` has NO clean `v2.15.x` multi-arch tag (only `:latest` + `:v2.15.0-build.NN`), so it can't follow v2.15.1; instead `metrics.enabled=false` (exporter Deployment never created, so the arch issue can't bite) and pin the unused override to `:v2.15.0-build.32`. Never trust `:latest` on a pre-baked box. (0e9a1fd) |
| 2026-07-08 | Falco 0.39.2 (chart 4.16.0) DaemonSet CrashLoopBackOff on every node: modern_ebpf probe fails `scap_init` ("An error occurred in an event source, forcing termination" → "Initialization issues during scap_init") on Ubuntu 26.04 kernel 7.0.0. NOT a config problem — BTF present (`/sys/kernel/btf/vmlinux`), pod privileged, driver already `modern_ebpf` (CO-RE). Falco 0.39.2's bundled libscap/libpman predates kernel 7.0. A live upgrade to 0.43.1 revealed it is a BREAKING migration, not a version bump: `--cri` CLI flag removed (`Option 'cri' does not exist`); container.* fields moved to a separate container plugin in Falco 0.41+, so the bundled rules fail to compile (`filter_check called with nonexistent field container.id`); falco.yaml/rules schema changed — it never reached scap_init, so even 0.43.x kernel-7.0 support is unconfirmed. | Disabled Falco in gitops (`falco.yaml` wrapped in `{{- if false }}` with rationale) to stop the crashloop; runtime coverage remains via trivy-operator (vuln scan), kyverno (policy), NetworkPolicy, and STRICT mTLS. A real fix is a dedicated Falco 0.43+ migration (args + falco.yaml + rules + container plugin via falcoctl) with `scap_init` verified on kernel 7.0, OR pin the box to a 6.x LTS kernel. |

| 2026-03-09 | 14-gitops-bootstrap.sh `GITEA_ADMIN_PASSWORD="gitea-admin"` hardcoded -> mismatch with actual generated password | Dynamically query with `kubectl get secret gitea-admin -n devtools -o jsonpath='{.data.admin-password}' \| base64 -d` |
| 2026-03-09 | oauth2-proxy-secrets missing `client-id` key -> `CreateContainerConfigError` | Add `--from-literal=client-id=oauth2-proxy` when creating secret in 11-3-keycloak-clients.sh |
| 2026-06-17 | 11-3이 apisix용 `platform-system/gitea-oidc-secret`(키 client_secret/session_secret)과 storage용 `velero-ui-oauth`(키 client_secret)을 생성하지 않아 apisix·velero-ui가 `CreateContainerConfigError` | 11-3 Group B에 gitea(platform-system) 추가 + storage/velero-ui-oauth 생성 추가로 해소 |

| 2026-03-23 → 08-04 | `set -o pipefail` kills a script from **two** directions, and knowing only the first is what caused the second. (a) A `grep` with no match exits 1 and takes the pipeline with it. (b) A **successful** downstream command that closes the pipe early — `awk '{print; exit}', `head -1` — makes the still-writing upstream take SIGPIPE, and pipefail reports that as failure: measured rc=141 (128+13) on `apt-cache madison kubelet \| awk '... exit'`. On 2026-08-04 that aborted 03-k8s-install.sh on all six nodes before its own error handling could print anything, so the log showed a banner and then silence, and up.sh spent all 6 Phase 1 attempts on nodes with no kubelet | For (a) `\|\| true`. For (b) **`head -1` is not the fix — it is the cause**, which the earlier version of this row and the CLAUDE.md rule both got wrong by prescribing it. Take the first match inside the filter without exiting (`awk '$3 ~ v && !seen {print $3; seen=1}'`), or terminate the whole substitution with `\|\| true`. Symptom to recognise: a script that dies with no message at all right after a pipeline — see the 2026-07-30 join row for the same signature |
| 2026-03-29 | `kubeadm join` without `--node-name` registers worker as `vagrant-ubuntu` (OS default hostname) instead of `narwhal-worker-X` | Always pass `--node-name $(hostname)` to `kubeadm join`. Even if `01-prerequisites.sh` sets hostname, kubeadm may use the OS default. Fix: `02-join-worker.sh` now includes `--node-name`. Recovery: `kubectl drain + delete node`, then `kubeadm reset -f && kubeadm join --node-name narwhal-worker-X` |
| 2026-04-06 | Keycloak StatefulSet 인데 `kubectl exec -n iam deploy/keycloak` 사용 | Keycloak Operator는 StatefulSet 생성. `kubectl exec -n iam keycloak-0 -c keycloak` 사용 |
| 2026-04-06 | `kcadm.sh` protocol mapper config 형식 오류 `{"config":{...}}` | 올바른 형식: `-s 'config={"key":"value"}'` (NOT `-s '{"config":{...}}'`) |
| 2026-04-06 | bash 함수 내 `echo` stdout 캡처 문제 → secret 변수에 로그 메시지 혼입 | 함수 내 모든 로그용 echo에 `>&2` 추가. `$(func)` 캡처 시 stdout만 반환됨 |
| 2026-04-06 | `KC_HOSTNAME_PORT` 은 Keycloak v1 옵션 (v26에서 무시됨) | Keycloak v2(v26+): `hostname.hostname` + `proxy.headers: xforwarded` 사용. 포트 제거는 X-Forwarded-Port 헤더로 처리 |
| 2026-04-06 | APISIX IC + ExternalName 서비스: `conflict headless service and backend resolve granularity` | ExternalName 서비스는 IC가 headless로 간주하여 `resolveGranularity: service`와 비호환. IC가 동기화 실패 시 APISIX admin API에 직접 패치로 우회 |
| 2026-04-06 | APISIX IC route 동기화 실패 시 admin API의 기존 route는 유지됨 | IC 실패 중에는 수동 admin API 패치가 overwrite되지 않음. `curl PATCH /apisix/admin/routes/{id}` 로 plugin 직접 추가 가능 |
| 2026-04-06 | Keycloak issuer URL에 `:9443` 포트 포함 → OpenBao OIDC discovery 실패 | APISIX `proxy-rewrite` plugin으로 `X-Forwarded-Port: 443` 설정 필요. IC 실패 시 admin API 직접 패치 |
| 2026-04-06 | API server manifest OIDC 플래그 중복 추가 (yq append 반복 실행 시) | 스크립트 실행 전 `grep -c 'oidc-issuer-url'` 으로 중복 확인. Python으로 중복 제거 후 재실행 |
| 2026-04-06 | Keycloak Operator-managed NetworkPolicy `keycloak-allow-hbone` blocks service port 8080 | Operator creates NetworkPolicy allowing only HBONE port 15008. When Keycloak is opted-out (`istio.io/dataplane-mode: none`), clients must connect via plain TCP 8080, not HBONE. Must create separate `keycloak-allow-plain` or add 8080 ingress rule to the NetworkPolicy |
| 2026-04-06 | StatefulSet pod stuck in old revision (ztunnel blocking probe) causes RollingUpdate deadlock | If pod is not Ready (startup probe fails), StatefulSet RollingUpdate cannot proceed even after CR/StatefulSet update. Old revision pod keeps running with wrong config. Fix: `kubectl delete pod keycloak-0 -n iam` to force recreation with new spec |
| 2026-04-06 | APISIX IC sets `nodes: []` for ExternalName service with `resolveGranularity: service` | IC cannot resolve ClusterIP for ExternalName (no ClusterIP). Remove `resolveGranularity: service` from ApisixRoute backend — IC then follows CNAME and sets FQDN as upstream node (same as ArgoCD pattern) |
| 2026-04-06 | Keycloak container image has no `curl` or `wget` (UBI9 minimal) | Cannot use `curl`-based exec probes. Use `bash /dev/tcp` or rely on Operator-managed httpGet probes. Operator ignores container probe overrides in `unsupported.podTemplate` |
| 2026-04-07 | APISIX container has no `curl` — `kubectl exec deploy/apisix -- curl` fails | Use master-1's curl directly: `curl http://$(kubectl get svc apisix-admin -n platform-system -o jsonpath='{.spec.clusterIP}'):9180/apisix/admin/...` |
| 2026-04-07 | nfs-quota-agent v0.2.1 HTML embeds Go raw-string-literal syntax `` ` + "`" + ` `` instead of actual backtick chars → JS SyntaxError → dashboard shows "Loading..." forever | Apply APISIX `serverless-post-function` body_filter with Lua `ngx.re.gsub` to replace all 22 occurrences at the gateway layer. **RESOLVED 2026-07-19 by upgrade to v0.3.0** (emits real backticks — verified 0 bug patterns / 17 real template literals through APISIX SSO); the gateway body_filter workaround was removed. |
| 2026-07-19 | Kyverno Enforce grandfather trap: nfs-quota-agent's running v0.2.1 pod (privileged + hostPID for XFS project quotas) was admitted BEFORE the `disallow-privileged-containers`/`disallow-host-namespaces` Enforce policies existed, so it kept running — but the v0.3.0 upgrade rollout hit `ReplicaFailure: FailedCreate` (webhook denied the NEW pod). A grandfathered privileged/hostPID/hostPort workload looks healthy until its next rollout/reschedule silently fails | When upgrading/rescheduling any privileged, hostPID/hostNetwork/hostIPC, or hostPort workload, FIRST confirm its namespace is in the relevant Kyverno policy `exclude` list — a running pod is NOT proof the policy allows it. Added `nfs-quota-agent` to both privileged + host-namespaces exclusions. Same class as the 2026-07-10 `=()` anchor lesson: Enforce policies bite at admission, i.e. on the NEXT create, not on already-running pods. |
| 2026-04-07 | APISIX IC stores `$secret://kubernetes/k8s-1/...` refs as plaintext in etcd when secret manager is not configured → openid-connect plugin sends literal string to Keycloak → `unauthorized_client` | Configure APISIX k8s-1 secret provider via admin API, OR patch the route directly via admin API with the plaintext secret (APISIX auto-encrypts on write) |
| 2026-04-07 | APISIX IC `ResourceSyncAborted` for ExternalName backends (no endpoints) — IC cannot sync route but doesn't re-create after admin API DELETE | Maintain these routes via admin API in provisioning scripts (11-3-keycloak-clients.sh). ApisixRoute YAML is documentation-only for these cases |
| 2026-04-07 | Grafana `grafana.ini` in Helm values generates `GF_AUTH_*` env vars; additional `kubectl patch deployment` adding same env vars → duplicate entries → ArgoCD `ComparisonError: duplicate entries for key` | Remove kubectl patch; manage all Grafana OAuth config solely via `prometheus-stack.yaml` grafana.ini. If live deployment has duplicates: Python dedup + `kubectl replace` |
| 2026-04-07 | `vault.hashicorp.com/agent-inject: "true"` annotation without `agent-inject-secret-*` path → OpenBao Agent Injector still injects init container → 403 `permission denied` on Kubernetes auth → pod stuck `Init:0/1` | Remove vault annotations when app uses `envFrom.secretRef` directly. Only add vault annotations when actual `agent-inject-secret-*` paths are configured |


### Cloud Deployment Mistakes (Kakao Cloud, 2026-07)

> Everything here surfaced only on a plain cloud image. The pre-baked Vagrant box provided
> each of these for free, so nothing in the repo had ever had to establish them.
> Architecture and the fixes in context: [`cloud-deployment.md`](../kakao/cloud-deployment.md).

| Date | Mistake | Fix |
|------|---------|-----|
| 2026-08-04 | "airgap" covered container images only. `03-save-helm-charts.sh` had bundled 24 chart tarballs since it was written and nothing read them; every script did `helm repo add <public url>`. Binaries (helm, cilium-cli, hubble, yq) and ten YAML manifests came from get.helm.sh and raw.githubusercontent.com at install time. The squid proxy made it all work, so the gap read as flakiness — two runs died on a helm fetch and a retry appeared to fix it | Resolve charts, binaries and manifests from the bundle with no internet fallback (`lib-charts.sh`), collected by `07-save-binaries.sh`. R28/R29 fail the build if a public fetch returns. A bundle nobody reads is indistinguishable from no bundle |
| 2026-08-05 | Ran `07-save-binaries.sh` for amd64 only, so the arm64 bundle the Vagrant path uses sat at bin=0 manifests=0 while amd64 had 4 and 10. Every local install would have died at the first `install_bin helm`, and no check caught it: they resolve whichever architecture matches the host, which here is the broken one, and only counted images | Verify both bundles, not the convenient one (R30). Two deployment targets means two of everything |
| 2026-08-02 | The helm 503 through squid came back on a clean run even though the IPv4-only DNS fix was in place and verifiably working — the same proxy answered 200 when probed by hand minutes later. `setup-bastion-proxy.sh` restarted squid **before** installing and starting dnsmasq, so squid came up pointing `dns_nameservers` at a resolver that did not exist yet, and cached the failure | Start dnsmasq first, squid second. When one daemon is configured to resolve through another on the same host, the order they are started in is part of the configuration — a fix that is present but sequenced wrong looks exactly like no fix at all |
| 2026-08-02 | The `base` stage reported `FAILED: common/02-containerd.sh` on worker-3, whose own log showed the full chain finishing cleanly. Killing the local driver does not kill the remote shell — it runs to completion — so resuming started the same stage on a node still working through the previous attempt, and the two fought over the apt lock. The loser's failure was reported against a node that was fine | Serialise per node: `sudo flock -w 7200 /var/lock/narwhal-stage.lock env … sh -c '…'`. flock takes its command as argv, so it adds no quoting layer. A driver that survives being killed needs mutual exclusion, not just idempotent steps |
| 2026-08-02 | Replacing the six instances, `master[2]` failed with `GetNetworkInterface: 404 Not Found` — but the instance had actually been created on the Kakao side. The provider died before writing it to state, leaving an orphan that held the pinned IP, so the retry failed with `Fixed IP address 172.17.0.12 is already in use`. `tofu import` does not rescue it either: import fills volume size and security group differently from the config, and the next plan then fails with `Attached volumes cannot be updated size` / `Changing the initial security group is not allowed` | Treat a failed `kakaocloud_instance` create as possibly-succeeded: list instances over the API, and if the name is there, `tofu state rm` (if imported) then DELETE it and re-apply. Import is not a usable recovery for this resource type |
| 2026-08-01 | Pinned mountd with `RPCMOUNTDOPTS` in `/etc/default/nfs-kernel-server` and it was silently ignored — that file is the sysv-era path, while the systemd units read `/etc/nfs.conf`. `rpcinfo -p` showed mountd on 49793/53677/… while lockd, pinned by sysctl, sat correctly on 4045. The v3 mount then timed out: the client asks rpcbind for mountd's port, gets one the security group drops, and waits | Write `/etc/nfs.conf.d/*.conf` instead, and **assert the result** — `01-nfs-server.sh` now fails loudly if `rpcinfo` does not report 20048. A config file that is read by nothing looks identical to one that works until something asks for the port |
| 2026-08-01 | The v3 switch was declared done when the StorageClass showed `nfsvers=3`, but the first PVC stayed Pending for 10 minutes and Phase 2 died on its very first script. The storage class was only the half that is easy to see; the RPC plumbing underneath it had never been exercised | For a protocol change, the passing test is a real mount and a real write from a real node — not the setting that requests it |
| 2026-08-01 | Re-running `init` on an already-initialized master died at `cp: overwrite '/home/vagrant/.kube/config'?`. Both init and join copy admin.conf with `cp -i`, which asks a question there is no tty to answer, so the script fails. It never showed up before because it only triggers on the second run — and both scripts have an explicit idempotency guard that skips kubeadm and falls straight through to exactly this line, so re-running is a designed path, not an accident | `cp -f`. An interactive flag in a script that runs over ssh with no tty is a latent failure waiting for the first retry; grep the tree for `-i` on cp/mv/rm when auditing re-runnability |
| 2026-08-01 | `03-cni-install.sh` died on `curl: (56) CONNECT tunnel failed, response 503` fetching helm, while a direct curl from the bastion to the same URL returned 200. `get.helm.sh` answers with an IPv6 address first and the VPC has no IPv6 route; curl falls back to IPv4, squid tries the address it was given and fails. Squid 6 removed `dns_v4_first`, so there is no proxy-side knob | Point squid at the local dnsmasq and set `filter-AAAA` there. Same root cause as the `Acquire::ForceIPv4` that 01-prerequisites.sh already sets for apt — when one client on a host needs IPv4 pinned, every client on it does |
| 2026-08-01 | Added a success marker so a dropped ssh could not be mistaken for failed work, and wrote it as a separate line after the command instead of `&&`-ing it — so it was touched even when the scripts had failed. The `init` stage then reported ok on a run whose log said `FAILED: cluster/03-cni-install.sh`, and the cluster went on to phase 2 with no CNI at all | A marker written unconditionally is worse than no marker: it converts a loud failure into a silent one. Also clear the previous attempt's marker before starting, or a retry inherits evidence from the run before it |
| 2026-08-01 | The `base` stage reported `172.17.0.22 FAILED` on a node that had in fact completed every script — its log ended with "Boot-Heal Install Done" and no FAILED line, and kubeadm, yq, jq, containerd and the pinned statd port were all in place. ssh returned non-zero after the work finished, and the driver read the transport's verdict as the work's verdict, so `mark_done` never ran and the whole stage was retried | Have the node record its own success: the remote shell touches `<stage>.ok` as its last act, and when ssh reports failure the driver reconnects and asks the node. A dropped connection after completion is otherwise indistinguishable from a failure — and over a bastion hop, dropped connections are routine |
| 2026-07-31 | NFSv4.1 deadlocked the whole cluster's storage, and it was mutual: the server sat in `nfsd4_destroy_session -> nfsd4_probe_callback_sync -> __flush_workqueue` waiting for the callback workqueue to drain, while the client sat in `nfs4_destroy_clientid -> rpc_wait_bit_killable` waiting for the server's reply — the work item the server was flushing was the RPC to that same client. The workqueue is shared across clients, so one unresponsive node froze NFS for all of them: 8 of 10 unhealthy pods were on that node and GitOps never bootstrapped | Move the storage class to `nfsvers=3`, which has no clientid, sessions or callback workqueue, so the cycle cannot form. Rebooting one side is not a fix — the survivor re-establishes the same session pair within minutes, so both ends have to be cleared together |
| 2026-07-31 | Rebooted both deadlock ends to clear it, then went to apply the v3 change — and the wedge re-armed before I got there, because the nodes came back and kubelet immediately remounted using the still-v4.1 storage class | Change the configuration BEFORE clearing the deadlock, not after. A reboot only buys the window between boot and the first remount |
| 2026-07-31 | `mountOptions` are copied into each PV's spec at provision time, so editing the StorageClass does nothing for volumes that already exist — all 15 PVs still carried `nfsvers=4.1`. A storage-protocol change is therefore not a config edit; it needs either a per-PV patch with everything scaled down, or fresh nodes | Validate a mount-option change on a build where no PV exists yet. When a setting is baked in at creation, "I changed the default" and "the running system uses it" are different claims |
| 2026-07-31 | The security group drops unlisted ports rather than refusing them, so any server-to-client callback on an unpinned port hangs instead of failing fast. This did NOT cause the v4.1 deadlock (the v4.1 backchannel rides the existing connection — `callback address: <node>:0`), and asserting that it did was wrong. It does apply to NFSv3's NLM lock grants, which do open a separate connection | Pin mountd/statd/lockd and open exactly those ports. Separately: `callback address: ...:0` in `/proc/fs/nfsd/clients/*/info` is what distinguishes a backchannel from a real callback connection — check it before blaming the firewall |
| 2026-07-30 | Fixed the node-resolution gap by enumerating all 14 service names in each node's `/etc/hosts`. It worked and it was the wrong shape — the list has to track every route ever added, and it does nothing for squid, which resolves on behalf of any proxied request, so the original HTTP 000 would have returned the moment a request went through the proxy. Vagrant never did this: it routes the whole zone to master-1's dnsmasq via systemd-resolved, and the kakao branch had simply skipped that block | Run dnsmasq on the bastion (`address=/${DOMAIN}/<worker-lb-vip>`) and point each node's systemd-resolved at it with `Domains=~${DOMAIN}`. One wildcard zone covers the nodes, squid, and every route added later. The bastion is the right host: it answers before the cluster exists, and squid lives there too. When a fix needs a list of the things it must cover, look for the layer where the list is unnecessary |
| 2026-07-30 | `11-4-keycloak-apiserver.sh` probed the OIDC discovery URL 15 times, got HTTP 000 every time, then WARNed and **skipped apiserver OIDC activation** — a cluster that finishes "successfully" with no OIDC login. `NO_PROXY` in `configure-node-proxy.sh` ended in a hardcoded `.local.narwhal.internal`, the Vagrant domain, so on Kakao the request went out through squid, which cannot resolve a name that exists only in the nodes' `/etc/hosts`. Squid answered `ERR_DNS_FAIL` behind the 000 | Derive it from `${DOMAIN}`. Diagnosing this cost three wrong turns — DNS, then the LB, then TLS — because `curl -v` was the first thing that showed the request going to `172.17.0.147:3128` at all. When a request fails oddly, check *who* is resolving the name before checking whether the name resolves |
| 2026-07-30 | Only `harbor.${DOMAIN}` was written into the nodes' `/etc/hosts`, guarded by `grep -q harbor.${DOMAIN}` — so adding a second name would never have taken effect either. The Phase 2 scripts curl service URLs from the NODE, not from a pod, so the CoreDNS hairpin zone does not cover them | Write all 14 service names between `# BEGIN/END narwhal-services` markers and delete the block before rewriting. A presence check keyed on one item is not idempotence for a list |
| 2026-07-30 | `02-join-control-plane.sh` reported failure on a node that had genuinely joined — new control-plane instance created, etcd member added, apiserver patched and back, kubeconfig written. Its last line was a bare informational `kubectl get nodes`, so that listing decided the exit status; it inherited `KUBECONFIG=~/.kube/config-local`, which only master-1 has, fell back to `localhost:8080` and printed "connection refused". The same script's already-joined branch had `\|\| true` on the identical call | End with `kubectl --kubeconfig=/etc/kubernetes/admin.conf ... \|\| true`: never let a listing decide success, and never rely on an ambient KUBECONFIG that only one node has. Four more scripts (04-addons, 07-cnpg, 12-gitea, 13-argocd) ended the same way and were guarded too |
| 2026-07-30 | `02-join-control-plane.sh` died on master-2 with no error at all — the log simply stopped after its previous echo, which reads as a hang. `LOCAL_IP="${NODE_IP:-$(ip -o addr show \| grep 192.168.56. \| ...)}"` fell through to the Vagrant fallback because the cloud driver never passed `NODE_IP`, and under pipefail a non-matching grep aborts the assignment. The script's own comment claimed "Cloud: NODE_IP is supplied explicitly" — nothing supplied it | Pass NODE_IP per node from the driver (`node_env` now takes the node's address), and add `\|\| true` plus an explicit emptiness check so the next occurrence says which variable is missing instead of vanishing. Same rule as CLAUDE.md's grep/pipefail entry, three years of scripts later |
| 2026-07-30 | The control-plane VIP looked dead from master-2 (`curl` 000), yet the apiserver answered 200 on master-1's localhost. The NLB target group had all three masters as members and **no health monitor**, so ROUND_ROBIN sent ~2 of every 3 connections to nodes with nothing listening — and `kubeadm join` goes through that VIP. A single probe reads as "VIP down"; nine probes showed 2 successes | Attach `kakaocloud_load_balancer_health_monitor` (TCP) to every target group, so a member with no listener leaves rotation. Probe a suspect endpoint repeatedly before concluding it is down — intermittent and dead look identical at n=1 |
| 2026-07-30 | Nothing in the repo installed `yq`, and three scripts edit manifests with it. On a plain cloud image `patch-apiserver-memory.sh` died with `yq: command not found` **immediately after a successful `kubeadm init`** — which reads as a control-plane failure, not a missing package. `11-4-keycloak-apiserver.sh` would have hit the same wall later in Phase 2. It must be mikefarah/yq: apt's `yq` is a Python jq wrapper that cannot do the `-i` in-place edits these scripts use | Install the Go binary in `01-prerequisites.sh` and assert `yq --version` reports mikefarah, so a jq wrapper cannot masquerade until the first `-i`. Same class as nfs-common and br_netfilter: the pre-baked box supplied it, so the need was never written down |
| 2026-07-30 | `jq` was present on both images but only as a transitive dependency — nothing in the repo ever asked for it, while `11-3-keycloak-clients.sh` drives Keycloak entirely through it. One base image that stops pulling it in breaks Phase 2 with no warning | Install it explicitly. A tool that happens to be there is not a dependency that is satisfied; the regression check now flags any manifest tool the scripts use but no script installs |
| 2026-07-30 | The `registry` stage installed docker and stopped there, so `04-bootstrap-registry.sh` stood up a working registry:2 and `05-load-images.sh` immediately died on `ERROR: skopeo not installed`. The two airgap scripts need different tools — 04 drives a container runtime, 05 pushes OCI layouts with skopeo — and the failure came after the 6.8 GB transfer had already succeeded | Install both up front and check for each separately. "The registry is running" says nothing about whether anything can be pushed into it; a bootstrap step that succeeds is not evidence the load step will |
| 2026-07-30 | The new `registry` stage reported `script sync FAILED` and the driver still exited 0. The `all` branch was a single `stage_a && stage_b && ...` chain, which is the last command in its `case` arm — a false result ends the chain but the for-loop moves on and the closing `log "done"` sets rc=0. Byte-for-byte the `up.sh` bug from 2026-06-17, reintroduced in new code | Route every stage through a `run_stage` wrapper that exits 1 on failure. A pipeline whose result is never read is not error handling; check that a deliberately failing stage actually yields a non-zero exit |
| 2026-07-30 | `rsync -a src/ host:~/scripts/airgap/` failed with `mkdir "/home/ubuntu/scripts/airgap" failed: No such file or directory` while the sibling call to `~/airgap-bundle/` succeeded. rsync creates only the LAST component of a destination path, so a one-level destination works and a two-level one does not | `ssh mkdir -p` the parent first (or use `--rsync-path`). The asymmetry is the trap: one working rsync in the same function is no evidence the next one works |
| 2026-07-30 | The clean-install driver printed `sudo -E PROVIDER=kakao ...` as the operator's next step in two scripts. A 2026-07-26 row already records that `sudo -E` is refused on these images, but only the code was fixed — the instructions kept telling people to run the broken form, which silently drops PROVIDER and exits 0 | When a variable-passing convention changes, grep the `echo` lines too. Guidance that contradicts the code is worse than no guidance: it is followed |
| 2026-07-30 | I documented the teardown remedy before executing it — twice. First "it takes over an hour, wait it out", then "leave the VPC in place and apply". The second was tested minutes later and failed on `VPC status is 'PENDING_DELETE'` | Do not write remediation you have not run. A workaround in a README reads as verified, and the next person pays for the difference |
| 2026-07-30 | A waiter built to poll a stuck resource watched for only one of the two documented outcomes. The API's own error says the state ends in deletion **or** a revert to ACTIVE; the loop only checked for the resource disappearing, so the ACTIVE branch would have spun to its 8h ceiling | When an API tells you the states it can settle into, handle every one of them. Read the error text as a spec, not as an apology |
| 2026-07-29 | `10-dnsmasq.sh` exits early on `PROVIDER=kakao` with a comment claiming DNS is "handled by /etc/hosts + CoreDNS" — but nothing ever wrote the CoreDNS half, and `10-worker-dns.sh` repeats the same claim while also exiting. `*.<DOMAIN>` was NXDOMAIN inside the cluster, so APISIX could not resolve the Keycloak discovery URL and all five gateway-OIDC routes (prometheus, alertmanager, gitea, hubble, nfs-quota) returned 500. Latent since the first Kakao deploy; the old domain failed identically, so the domain migration only exposed it | Implement the Kakao branch: a CoreDNS zone answering the whole domain via the `template` plugin with the apisix-gateway ClusterIP. Wildcard, not a hosts list — Vagrant's list falls through to dnsmasq so omissions stay invisible, while on Kakao an unlisted name has nowhere to go. A comment describing intent is not an implementation |
| 2026-07-29 | After the domain change, every host failed TLS with `tlsv1 alert internal error` while the Certificate and ApisixTls CRs were both correct on the new domain. `apisix-ingress-controller` had gone idle holding a stale cache — `metadata.generation: 6` vs `status.observedGeneration: 1`, no logs for 40 minutes — so APISIX still served `*.local.narwhal.internal` config: SSL SNI plus all 21 routes | Compare generation against observedGeneration before suspecting cert-manager, and query the APISIX admin API for what is actually loaded rather than trusting CR status ("Sync Successfully" was two days stale). `rollout restart deploy/apisix-ingress-controller` forces a full resync |
| 2026-07-29 | The Kakao CoreDNS hairpin zone answered `A` only, so every `AAAA` query fell off the end of the zone as SERVFAIL. Stub resolvers ask for both families together, so half of every lookup failed — `nslookup` exited 1 on a name that resolved perfectly. Masked because APISIX's Lua resolver only asks for A | Add `template IN AAAA { rcode NOERROR }` so the v6 half is NODATA, which is what a v4-only name actually means. Never judge resolution by a resolver tool's exit code — check the answer |
| 2026-07-29 | The same script's idempotency guard matched on the zone header plus the APISIX IP alone, so editing the zone *body* was reported "already present" and silently never applied — the AAAA fix appeared to deploy while the cluster kept the old zone | Compare the fully rendered Corefile against the live one. A guard that checks a proxy for the content instead of the content cannot see a content change |
| 2026-07-29 | The documented clean-install sequence (`stage-kakao-nodes.sh` then `provision-kakao.sh all`) could not produce a working cluster from scratch. Two prerequisites existed only as tribal knowledge: the bastion squid proxy (the nodes have no public IP and provider 0.4.4 has no NAT gateway, so `01-prerequisites`' first apt-get has nowhere to go) and the bastion registry (the `mirror` stage points containerd at a registry nobody had filled). Both had scripts; nothing called them, and no doc listed them. Invisible because the cluster was only ever built by hand, step by step | Add `proxy` and `registry` stages to the driver and put them at the front of `all`. The driver exists precisely because the cloud has no Vagrantfile to encode ordering — a step that lives only in an operator's head is a step the driver is missing |
| 2026-07-29 | `tofu destroy` cleared 52 of 53 resources in ~15 min, then spent 1h54m on the VPC alone before being interrupted. `tofu refresh` afterwards kept the VPC in state and `tofu plan` reported a clean `47 to add, 0 to destroy`, so reusing it looked safe — but `apply` died with `create resource: kakaocloud_subnet / VPC status is 'PENDING_DELETE'`. The VPC was neither alive nor gone, and nothing in plan output says so | Read `provisioning_status` off the resource, never judge a Kakao VPC from plan output. `PENDING_DELETE` cannot host new subnets, so waiting for the delete to land is the only move — a re-run of `destroy` achieves nothing the wait does not |
| 2026-07-26 | `02-containerd.sh` pinned `containerd=1.7.*`. On Ubuntu 24.04 the pin SUCCEEDS, pairing 1.7.12 with runc 1.3.4 — whose AppArmor profile denies runc the right to signal its own container init (`apparmor="DENIED" operation="signal" comm="runc"`). Containers cannot be killed: pods wedge in Terminating, nfsd/mount.nfs pile up in D state, the NFS server stops answering (master-1 could not mount its own export), PVCs time out, Postgres stalls mid-Liquibase, Phase 2 dies at 11-2-keycloak-config. One runtime bug presenting as four unrelated failures | Drop the pin — 24.04 offers 2.2.1 and 26.04 offers 2.x, so the repo default is right on both. Warn loudly if 1.7.x is installed. Already-wedged D-state processes need a REBOOT; `systemctl restart nfs-server` itself hangs |
| 2026-07-26 | Ubuntu cloud images boot with unattended-upgrades enabled and `apt-daily-upgrade.timer` fires within ~20 min. It took the dpkg lock (killing `02-containerd.sh` on two nodes) and separately upgraded+restarted squid on the bastion, making apt fail with "Connection refused" on all six nodes at once | `stage-kakao-nodes.sh` disables the timers, waits for any in-flight transaction, runs `dpkg --configure -a`. apt also gets `Acquire::Retries "5"` — every package on every node funnels through one proxy, and apt defaults to zero retries |
| 2026-07-26 | Nothing in the repo set `net.ipv4.ip_forward` or loaded `br_netfilter`; the box ships `/etc/sysctl.d/k8s-network.conf`. kubeadm init died at preflight | `01-prerequisites.sh` creates both files. Load the modules BEFORE applying the bridge sysctls — `net.bridge.*` keys do not exist until `br_netfilter` is in, and sysctl skips missing keys silently |
| 2026-07-26 | No script installed the NFS **client**; `01-nfs-server.sh` only sets up the server. csi-driver-nfs failed with "bad option; ... you might need a /sbin/mount.<type> helper program", which reaches the pod as a 110s mount timeout, not as a missing package | `01-prerequisites.sh` installs `nfs-common` on every node — the CSI node plugin mounts wherever the pod lands |
| 2026-07-26 | Scripts assume a `vagrant` **user**, not just the `/home/vagrant` path: `02-init-cluster.sh` chowns the kubeconfig and died with `chown: invalid user 'vagrant:vagrant'` | Create the account in `stage-kakao-nodes.sh` rather than patching each chown — it satisfies every such assumption at once, including any not found by grep |
| 2026-07-26 | `NFS_SERVER_IP` and `HOST_NETWORK_CIDR` defaulted to `192.168.56.*`: the StorageClass pointed at an address that does not exist on Kakao, and the NFS export ACL excluded the node subnet. Both were ALREADY parameterized — nobody had assembled the full env set for a non-Vagrant run | `provision-kakao.sh` derives them from OpenTofu state. StorageClass parameters are immutable, so a wrong one must be deleted, not re-applied |
| 2026-07-26 | `sudo -E` is ignored on these images (`sudo: preserving the entire environment is not supported`), so exported variables silently vanish — a mirror run quietly used the default `registry.airgap.local:5000` instead of the address it was given | Pass every variable as `sudo env VAR=... script.sh`. (`sudo -E VAR=value cmd` — an explicit assignment — does pass through) |
| 2026-07-26 | `06-configure-mirrors.sh` wrote the mirror host as `<REG>/<upstream>`, so containerd requested `<REG>/<upstream>/v2/<repo>` while `05-load-images.sh` had stored at `<REG>/v2/<upstream>/<repo>`. The registry returned its 404 HTML page, which reaches kubelet as `unexpected media type text/html for sha256:...: not found` — naming neither the URL nor the 404. The airgap mirror had never served a single pull | Host path must be `/v2/<upstream>` with `override_path = true`. Verify by grepping the registry log for `useragent="containerd`; if it is absent, the mirror is not being used |
| 2026-07-26 | `05-load-images.sh` pushed refs verbatim, but `images.txt` records them as the cluster reports them, so Docker Hub entries arrive bare. containerd resolves `busybox:1.28` to `docker.io/library/busybox` before consulting the mirror — 6 of 92 images unreachable, including APISIX and SeaweedFS | Normalize to the fully-qualified form on push |
| 2026-07-28 | `01-generate-image-list.sh --live` reads images off running pods, which structurally cannot see `pause` (sandbox image, never a pod container) or `kube-proxy` (Cilium replaces it). Control-plane tags also drifted a patch behind the kubeadm apt installs. 5 of the 7 refs in `kubeadm config images list` were missing from the bundle | Union `kubeadm config images list` and fail loudly if it cannot be read. Verify by stopping the proxy entirely and pulling — a live proxy hides mirror gaps behind internet fallback |
| 2026-07-28 | `02-save-images.sh` writes the bootstrap registry as a docker-archive, and that transport rejects an existing file. Every rebuild after the first reported `Failed: 1` and moved on, freezing `bootstrap/registry.tar` at the bundle's creation date while all other images refreshed | `rm -f` the tar before writing |
| 2026-07-27 | macOS bsdtar writes a binary AppleDouble sidecar (`._<name>`) for every file with extended attributes. A vendored chart shipped that way gave ArgoCD `charts/kong/templates/.__helpers.tpl`, and helm — which reads every file under `templates/` — died with "control characters are not allowed". 556 had reached the Gitea repo | `COPYFILE_DISABLE=1` plus a sweep after staging. Note `push-to-gitea.sh` mirrors with `cp -r`, which overwrites but never deletes — a bad file already committed must be removed from the repo directly |
| 2026-07-28 | `patch-apiserver-memory.sh` was called as `"$(dirname "${BASH_SOURCE[0]}")/..."`. Vagrant copies a provisioner to `/tmp/vagrant-shell` and runs it there, so the sibling was not beside it: `02-init-cluster.sh` aborted right after kubeadm init, and all five other nodes failed with "Failed to get join command". Hidden on the cloud path, which invokes scripts by full path | Use `/home/vagrant/scripts/...` absolute paths, matching every other cross-script reference in this tree |
| 2026-07-28 | The same script returned immediately after rewriting the apiserver's static pod manifest, straight into the window where kubelet restarts it. `03-cni-install.sh` ran next and died on its first kubectl (`connection refused`) — the cluster came up complete except for having no CNI, six nodes NotReady, coredns Pending | Poll `/livez` until it answers, then fail loudly on timeout. Sleep FIRST: `/livez` keeps answering until kubelet actually stops the old container, so an immediate poll matches the process being replaced |
| 2026-08-05 | The 2026-07-28 row above prescribes "stop the proxy entirely and pull" as the way to prove the bundle is complete. Measured on Kakao, that test does not hold: with squid stopped, a node still reached `get.helm.sh` (404) and `raw.githubusercontent.com` (301) directly — the VPC has its own egress path, so anything still fetching from the internet passes the test and fails on a real closed network. Same blind spot as the live proxy, one layer down | Block it at the node: `scripts/test/airgap-isolate-kakao.sh on` rejects everything leaving the box except the VPC, pod/service CIDRs and link-local. REJECT not DROP — Kakao security groups drop, and three incidents here were spent chasing a hang a fast failure would have named. Discriminator is `curl --noproxy '*'` to a public host: absence of a proxy is not absence of a route |
| 2026-08-05 | The apt bundle and the `AIRGAP=1` switch were finished — 140 .debs, a coverage scanner, a `_apt` readability check, default-route removal to enforce isolation — and wired into the **Vagrantfile only**. `provision-kakao.sh` never passed `AIRGAP`, `stage-kakao-nodes.sh` never staged `apt/`, and the amd64 bundle had no `apt/` at all (only arm64 did). So the Kakao "airgap" run reached the Ubuntu archive for every .deb and passed, because the VPC has its own egress. The first genuinely offline attempt died at `01-nfs-server.sh` fetching nfs-kernel-server | Pass `AIRGAP` in `node_env`, stage the bundle to `/srv/airgap/apt`, and check both with R31/R32. The general shape: a capability wired into one provider's driver and not the other's is indistinguishable from one that does not exist, and the provider with the working proxy is the one where nobody notices. When a feature lands, grep for every driver that runs the same scripts |
| 2026-08-05 | `07-save-apt-packages.sh --docker` hung with no output at all on `docker create`. Not slow, not a timeout — the CLI resolves registry credentials before pulling, `~/.docker/config.json` said `"credsStore": "desktop"`, and this host runs colima. The VM had internet, the daemon was healthy, `docker ps` worked, and load average was 0.19, so every obvious probe said the system was fine. The first fix guessed the helper was absent and skipped the bypass when `command -v docker-credential-desktop` succeeded — it *is* on PATH at /usr/local/bin, left by an uninstalled Docker Desktop, and simply never answers. Presence was never the test | Always run the builder with `DOCKER_CONFIG=$(mktemp -d)`; the image is public so no credential lookup is wanted at all (`AIRGAP_KEEP_DOCKER_CONFIG=1` opts back in). A probe that can itself hang is not a usable guard — remove the dependency instead of detecting it. Discriminator: `docker pull` emits zero bytes while `curl https://auth.docker.io/token` returns 200 from the same shell |
| 2026-08-05 | The airgap mirror had still never served a pull — the 2026-07-26 row below fixed the hosts.toml path, and the config that points containerd at those files was broken the whole time. Ubuntu's containerd 2.2.1 ships `config_path = '/etc/containerd/certs.d:/etc/docker/certs.d'` **already populated**, and `02-containerd.sh` only rewrote an *empty* value ("leave already-set paths alone"), so it never touched it. containerd takes a SINGLE directory there — the colon list is Docker's syntax — so it looked for a directory literally named `certs.d:/etc/docker/certs.d`, found none, and ignored every hosts.toml. Nothing failed, because upstream was always reachable | Normalise any value, not just an empty one, with a `(?<![\w])` lookbehind so `plugin_config_path` (NRI) keeps its own. Measured both ways on one node: colon form → `kubeadm config images pull` dies with `network is unreachable`; single path → all six images pull from the mirror, and the registry log goes from 0 containerd requests to 132. The 2026-07-26 discriminator (grep the registry log for `useragent="containerd`) is what found this — it works, and it is worth running after *any* mirror change, not just a hosts.toml one. General shape: a default value is not a correct value, and "already set" is not a reason to skip a rewrite |
| 2026-08-05 | Phase 2 got 40 minutes in — CNPG up, cluster healthy — and died on `Error: repo apisix not found`. `08-1-networking.sh` fetched the APISIX CRDs with `helm pull apisix/apisix-ingress-controller`, a repo-qualified ref that needs someone to have run `helm repo add` first, which on an airgapped node is nobody. R28 was written to catch exactly this class and matched only the literal string `helm repo add`, so the one line that actually reached the internet was the one it could not see | Unpack `$(chart apisix-ingress-controller)` from the bundle, which was carrying that chart all along. New R28b bans `helm pull`/`helm fetch` outright rather than hunting repo-qualified refs — `--show-only templates/x.yaml` is also `<word>/<word>`, so a ref-shaped pattern cries wolf. When a check is named for a class of bug, test it against a *different* member of that class than the one that prompted it |
| 2026-08-05 | With the mirror finally working, istiod, istio-cni, gitea and argocd-redis still sat in ImagePullBackOff — and the images were in the registry the whole time, on the exact tags the pods asked for. `06-configure-mirrors.sh` wrote a hosts.toml for a hardcoded five (registry.k8s.io, quay.io, ghcr.io, docker.io, cr.fluentbit.io) while the bundle carries eleven; registry.istio.io, docker.gitea.com, public.ecr.aws, gcr.io, mirror.gcr.io and reg.kyverno.io had no entry, so pulls from them went upstream. "Is the image in the mirror" and "will containerd look in the mirror for it" are different questions, and only the second one was ever wrong | Derive the upstream list from images.txt. Only a first segment with a dot is a registry — `busybox:1.28` has a dot but no slash, `apache/apisix` has a slash but no dot, and both are Docker Hub short forms. Fixing it took 13 certs.d entries and all 99 pods to Running. Discriminator for the next one: compare `ls /etc/containerd/certs.d` against the registry's own `_catalog` top level; a name in the catalog with no certs.d entry is a silent upstream pull |
| 2026-08-05 | Phase 2 printed "Complete" over a cluster with **zero** ArgoCD applications. `14-gitops-bootstrap.sh` pushes to the Gitea that `12-gitea.sh` stands up, and Gitea was still ImagePullBackOff from the mirror gap above, so the push failed against `localhost:3000`, 14 logged `WARN: had issues, continuing...`, and the ten platform apps it hands to ArgoCD were never created. Grafana, the dashboard, the portal and velero-ui all answered 503 with nothing behind them — and the regression check said `PASS T03 all ArgoCD apps Synced+Healthy`, because "every app is healthy" is vacuously true of no apps | Both scripts are now in CRITICAL_SCRIPTS: a step whose failure silently removes most of the platform is not non-critical. T03 counts first and fails below ten. Two general shapes: a "continue on error" default is only safe for steps whose output nothing else depends on, and any check phrased as "all X are Y" needs a floor on the number of X |
| 2026-08-05 | Adding three images to the bundle by hand produced layout directories named `..._v1.20.2atest`, and `skopeo copy` out of them then failed with `no descriptor found for reference "latest"`. `"oci:$dest:latest"` in zsh: `$dest:l` is a parameter modifier (lowercase), so it ate the `:l` and left `atest`. Then the cleanup made it worse — `for d in *atest` also matched the legitimately-named `docker.io_alpine_git_latest` and `gcr.io_kaniko-project_executor_latest`, truncating both to `_l` and breaking two working layouts | Brace the expansion: `"oci:${dest}:latest"`. And when a glob is meant to match damage, check it does not also match the healthy case — `*atest` cannot tell `_v1.20.2atest` from `_latest`. Both were restored by renaming back; verify with `index.json` presence, not with the directory name |
| 2026-08-06 | The two apt bundles disagreed by 9 packages — and the missing ones were dbus, dpkg, gcc-14-base, libapparmor1: base packages every fresh box needs. The arm64 bundle was built with `--ssh` against a provisioned node, the amd64 one with `--docker` in a fresh container, and the ssh path's dependency-closure loop (`apt-get install --download-only --reinstall "$p" \|\| true`) silently skipped what the node already had — the exact fresh-box hole the script's own comment claims that loop closes. Invisible on the Vagrant box, which ships those packages; fatal on a clean arm64 machine | The build environment is part of the bundle's correctness: a provisioned builder cannot see what a fresh target lacks. Prefer `--docker` (fresh container = fresh target) for both arches; arm64 rebuilt with it. Discriminator: diff the two bundles' package name lists — a per-arch count gap on arch-independent packages (`_all.deb`, dbus, dpkg) is a build-mode artifact, not an arch difference |
| 2026-08-07 | With Kakao deliberately un-isolated for a kagent deploy, `postgres:18.3-alpine` (not in the bundle) failed with `NotFound` — not `network unreachable` — even though the internet was open. The mirror missed as expected, but the hosts.toml fallback `server = "https://docker.io"` points at a *website* (`/v2/` answers 302 to marketing pages), so containerd's fallback resolved nothing and reported the miss as authoritative. Every other upstream serves the registry API on its own hostname; docker.io alone does not (`registry-1.docker.io`, 401 challenge). Invisible in every earlier state: isolated → no fallback possible; before that, `config_path` broken → hosts.toml never read at all | `fallback=registry-1.docker.io` special case in `06-configure-mirrors.sh`. The bug only exists in one quadrant — mirror active × internet open × image not mirrored — so test the fallback by pulling an unmirrored image while OPEN, not by auditing the file. Discriminator: `NotFound` with internet up = broken fallback host; `network unreachable` = isolation working |
| 2026-08-07 | A pre-push audit found `.airgap-registry-data/` — the local mirror's blob store, 6.8 GB — committed to a **public** repo's history in `fa0ab12`, 1960 of that commit's 2096 files. `.gitignore` already listed it: the entry was added *after* the commit, and gitignore has no effect on already-tracked paths. The push would have been rejected anyway (three files over GitHub's 100 MB hard limit), so the symptom would have been a confusing push failure rather than a leak, and the repo had silently grown to 5.79 GiB | `git filter-repo --path <dir> --invert-paths`; repo went 5.79 GiB → 3.84 MiB, commit count and every source file preserved. Before pushing to a public remote, check `git count-objects -vH` and `git diff --name-only <remote>/<branch>..HEAD \| wc -l` — a source-only change that touches 2000 files is data, not code. And after adding a `.gitignore` entry for something that already exists, run `git ls-files <path>` to confirm it is not already tracked |
| 2026-08-07 | Removing those blobs also deleted them from the **working tree**, which is where the running mirror registry served them from — local cluster's catalog fell 101 → 11. Re-running `05-load-images.sh` then reported `Loaded: 107 \| Failed: 0` while the registry stayed broken and every manifest 404'd. The registry runs with `blobdescriptor: inmemory`: it answered skopeo's "do you have this blob?" from a cache describing files that no longer existed, so skopeo skipped every upload and called it success. Restarting the registry alone did not fix it either — the tags still pointed at revisions whose blobs were gone | Restart the registry **first**, then reload; the second run wrote 812 blobs and the catalog returned to 107. Discriminator for a load that claims success but serves nothing: count blobs on disk before and after (`find .../blobs -name data \| wc -l`) — a push that adds zero blobs did not happen, whatever it printed. Deleting files under a running registry poisons its cache; treat that store as live state, not as files |
| 2026-08-07 | Two follow-ups to the 6.8 GB scrub, both from trusting a mechanism without testing it. **(1)** `git tag pre-scrub-backup HEAD` was reported to the user as a safety net; `git filter-repo` rewrites *every* ref including tags, so both "backups" ended up pointing into the cleaned history. There was never a way back — the recovery worked only because the registry was rebuilt from the bundle independently. A real backup before a history rewrite is a clone or a bundle file, never a ref in the repo being rewritten. **(2)** The new R35 check (`git ls-files \| git check-ignore`) reported nothing on a repo that provably had the problem: git never considers a *tracked* path ignored, so the check needs `--no-index` — the case its own man page is written for | Both caught by testing the guard against a reproduction of the incident rather than against the fixed repo. With `--no-index` the check immediately found six more real ones (`.bkit/` tool state, tracked and gitignored since March), now untracked. A guard that passes on a healthy repo has proven nothing; build the broken case and make it fail first |
| 2026-08-08 | The pre-push audit flagged `gitops/resources/kubemetal.yaml`'s S3 key as a hardcoded credential and the fix removed it, documenting `openssl rand` instead. Reading the **generator** repo afterwards showed that verdict was wrong in an instructive way: the source file says in a comment that the value is a placeholder for a SeaweedFS S3 gateway running with IAM unconfigured — it accepts anything, and the value exists only to fill the signature header boto3 requires. Worse, kubemetal's own D21 (2026-07-24) records that this exact value was *deliberately* aligned with fallback defaults in `access.rs`, `ingest_host.py` and `rag_host.py` after a mismatched `seaweedfsadmin` caused a bug — so renaming it across four files invites that desync back for no gain | The real defect was narrower than it looked: `render.sh` pipes through `kubectl kustomize`, which strips comments, so the note that makes the value safe to read never reaches the public repo. First fix was a header emitted by render.sh above the kustomize output, with narwhal's copy realigned byte-for-byte. **Then the ground moved**: kubemetal now installs only the agent (`kagent-up`, Helm OCI charts into the `kagent` namespace), so the whole MLflow/SeaweedFS/Prefect export was a dead integration path — opt-in, never enabled, namespace never created. Deleting it removed the flagged credential at the root and took ~1 GB of mlflow+prefect layers out of both bundles with it. Three lessons, in order of how much they cost: when a generated file looks wrong, read the generator before editing the output; a value that three other files hardcode as a fallback is a contract, not a secret; and before hardening a component, check it is still wired in at all — two rounds of careful fixes went into a path that no longer had a caller |
| 2026-08-08 | "Cleaned up and pushed" covered the **repo**; the clusters still carried what the repo work was about. kubemetal's D34 remote-reader had left a `kagent-remote-reader` ServiceAccount, a non-expiring `kubernetes.io/service-account-token` Secret, and cluster-wide read (`ClusterRole/view` plus a custom reader role) on **both** clusters — local since 2026-08-05, Kakao since 08-06. Deleting a manifest does not revoke a credential that was applied by hand | Check the cluster, not just the tree, whenever the finding is a credential. Two traps in the cleanup itself: **(a)** the vendor's own `teardown-remote-reader.sh` deletes the *reader* side (colima's agent, tools and kubeconfig Secret) unconditionally, so running it for the stale link would have severed the live one — the local objects had to go individually. **(b)** The tunnel the live link runs through binds loopback only (`127.0.0.1:6443`), which looked like it could not be reachable from the colima VM; a probe pod returned 200, because lima forwards loopback-bound host ports into the guest. Discriminator for "is this credential still used": find the *reader* side and prove it reaches the API, do not infer it from the credential's existence or from what a port binding looks like |
| 2026-08-08 | CI's ShellCheck job went red on two `SC2034 HELM_VERSION appears unused` warnings, hours after a full-repo audit here reported "shellcheck: 0건". Both were true: the audit ran `shellcheck -S error`, CI runs `--severity=warning`. A self-designed check that is weaker than the gate it is meant to anticipate gives the same clean output as a passing one, so the gap is invisible until CI says otherwise. (The variables themselves were dead since the airgap work replaced the helm download with `install_bin helm`; the version's single source is `07-save-binaries.sh`, and a second literal could only drift.) | Run the CI command verbatim, not an equivalent-looking one — for this repo that means reading `.github/workflows/lint.yml` and executing each job's `run:` block, which also caught nothing else this time but is the only way to know that. Generalisable: when local verification and a remote gate disagree, suspect the local invocation's flags before the code |
