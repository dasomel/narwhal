apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: istio-ambient-policies
  namespace: devtools
  annotations:
    # PeerAuthentication must be applied after istiod (wave 0) and ztunnel (wave 5) are healthy.
    argocd.argoproj.io/sync-wave: "5"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: http://gitea-http.devtools.svc.cluster.local:3000/gitea-admin/narwhal-gitops.git
    targetRevision: HEAD
    path: resources
    directory:
      include: "istio-ambient-policies.yaml"
  destination:
    server: https://kubernetes.default.svc
    namespace: istio-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
