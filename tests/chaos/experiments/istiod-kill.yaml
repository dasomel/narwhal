apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: istiod-kill
  namespace: chaos-testing
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - istio-system
    labelSelectors:
      app: istiod
