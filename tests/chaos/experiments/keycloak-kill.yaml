apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: keycloak-kill
  namespace: chaos-testing
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - iam
    labelSelectors:
      app: keycloak
